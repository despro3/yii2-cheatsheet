# -*- coding: utf-8 -*-
"""Вопросы для страницы «Проверка знаний Yii 2».

Вопрос — словарь:
    id       устойчивый идентификатор; по нему хранится статистика в браузере,
             поэтому менять его у существующего вопроса нельзя
    topic    тема из TOPICS
    text     формулировка
    code     ('php', '...') — необязательный фрагмент перед вариантами
    options  варианты ответа
    answer   индекс верного или список индексов, если верных несколько
    why      разбор: почему так, а не иначе
    ref      (страница курса без .html, подпись) — куда идти читать
    node     (узел справочника[, вкладка]) — необязательная вторая ссылка

Ответы к вопросам «что вернёт этот код» не сочинялись: каждый прогонялся
на настоящем Yii 2 (см. комментарий ПРОВЕРЕНО у таких вопросов).
"""

TOPICS = [
    {'id': 'start',  'title': 'Старт и устройство приложения',  'group': 'object'},
    {'id': 'http',   'title': 'Запрос, ответ, маршруты',        'group': 'http'},
    {'id': 'flow',   'title': 'Контроллеры, фильтры, модули',   'group': 'http'},
    {'id': 'views',  'title': 'Представления и виджеты',        'group': 'view'},
    {'id': 'output', 'title': 'Вывод данных и списки',          'group': 'view'},
    {'id': 'model',  'title': 'Модели, сценарии, формы',        'group': 'data'},
    {'id': 'valid',  'title': 'Валидация',                      'group': 'data'},
    {'id': 'ar',     'title': 'Active Record',                  'group': 'db'},
    {'id': 'query',  'title': 'Query Builder и DAO',            'group': 'db'},
    {'id': 'migr',   'title': 'Миграции',                       'group': 'db'},
    {'id': 'core',   'title': 'Компоненты, DI, события',        'group': 'object'},
    {'id': 'state',  'title': 'Кэш, сессии, куки',              'group': 'view'},
    {'id': 'sec',    'title': 'Безопасность и доступ',          'group': 'http'},
    {'id': 'api',    'title': 'REST и консоль',                 'group': 'object'},
    {'id': 'tools',  'title': 'Отладка, тесты, скорость',       'group': 'data'},
    {'id': 'misc',   'title': 'Хелперы и памятка',              'group': 'object'},
    {'id': 'world',  'title': 'Почта, языки, расширения',       'group': 'data'},
]


def q(**kw):
    return kw


QUESTIONS = [

# --------------------------------------------------------------- запрос и маршруты

q(id='http-pathinfo', topic='http',
  text='Входной скрипт лежит в подпапке `/admin`. Что выведет этот код при обращении '
       'к `https://example.com/admin/index.php/product?id=100`?',
  code=('php', 'echo Yii::$app->request->pathInfo;'),   # ПРОВЕРЕНО запуском
  options=['/product', 'product', '/admin/index.php/product', 'product?id=100'],
  answer=1,
  why='`getPathInfo()` отрезает строку запроса и путь до входного скрипта, а затем срезает '
      'ведущий слэш. Остаётся `product` — именно эту строку и разбирает `UrlManager`.',
  ref=('request-response', 'Запрос и ответ'), node=('http',)),

q(id='http-userip-proxy', topic='http',
  text='Приложение стоит за обратным прокси, `trustedHosts` не настроен. '
       'Что вернёт `Yii::$app->request->userIP`?',
  options=['Адрес клиента из заголовка `X-Forwarded-For`',
           'Адрес самого прокси',
           '`null` — Yii откажется гадать',
           'Первый адрес из списка `X-Forwarded-For`'],
  answer=1,
  why='Заголовки `X-Forwarded-*` — это то, что легко подделать, поэтому Yii читает их только для '
      'адресов, перечисленных в `trustedHosts`. Пока прокси не объявлен доверенным, вы видите его самого.',
  ref=('request-response', 'Запрос и ответ'), node=('http',)),

q(id='http-verb-rule', topic='http',
  text='В правилах маршрутизации есть строка с HTTP-методом. Что выведет генерация ссылки?',
  code=('php', r'''// config/web.php
'rules' => [
    'DELETE post/<id:\d+>' => 'post/delete',
    'post/<id:\d+>'        => 'post/view',
],

// в представлении
echo Url::to(['/post/delete', 'id' => 1]);'''),   # ПРОВЕРЕНО запуском
  options=['/post/delete?id=1', '/post/1', 'Пустую строку', 'Бросит исключение'],
  answer=1,
  why='`verb` смотрят только при разборе входящего запроса — при генерации его просто игнорируют, '
      'а само правило из перебора не выключают. Поэтому адрес соберётся по первому подходящему шаблону. '
      'Чтобы правило работало только на разбор, задайте ему `\'mode\' => UrlRule::PARSING_ONLY`.',
  ref=('routing', 'Маршрутизация и URL'), node=('routing', 'traps')),

q(id='http-strict-parsing', topic='http',
  text='`enableStrictParsing = true`, адрес `/posts/php` не подошёл ни одному правилу. Что будет?',
  options=['404', 'Запрос уйдёт в маршрут `posts/php`',
           'Редирект на главную', 'Возьмётся `defaultRoute`'],
  answer=0,
  why='Строгий разбор означает «маршрут существует, только если есть правило». Без строгого разбора '
      'путь трактуется как маршрут напрямую, и `/posts/php` ушло бы в `posts/php`.',
  ref=('routing', 'Маршрутизация и URL'), node=('routing',)),

q(id='http-route-slash', topic='http',
  text='Представление контроллера `post` внутри модуля `admin`. В какой маршрут превратится '
       '`Url::to([\'/post/view\', \'id\' => 1])`?',
  options=['admin/post/view', 'post/view', 'admin/view', 'Зависит от правил в `urlManager`'],
  answer=1,
  why='Ведущий слэш означает маршрут от корня приложения — модуль пропускается. Без слэша '
      '(`[\'post/view\']`) маршрут считался бы относительно текущего модуля, то есть `admin/post/view`.',
  ref=('routing', 'Маршрутизация и URL'), node=('routing',)),

q(id='http-json-out', topic='http', kind='many',
  text='Какими способами действие может отдать JSON? Отметьте все подходящие.',
  options=['`return $this->asJson($data);`',
           '`Yii::$app->response->format = Response::FORMAT_JSON; return $data;`',
           '`return json_encode($data);`',
           '`return $this->renderPartial(\'data\', $data);`'],
  answer=[0, 1],
  why='`asJson()` ставит формат и возвращает данные одним вызовом; второй способ — то же самое вручную. '
      'А `json_encode()` вернёт обычную строку, которую Yii отдаст как HTML — с заголовком `text/html` '
      'и без сериализации вложенных объектов.',
  ref=('request-response', 'Запрос и ответ'), node=('http',)),

# --------------------------------------------------------- контроллеры и фильтры

q(id='flow-missing-param', topic='flow',
  text='Действие объявлено как `public function actionView($id)`, а в запросе параметра `id` нет. '
       'Что произойдёт?',
  options=['`$id` будет `null`, действие выполнится',
           '`BadRequestHttpException` (400) ещё до входа в действие',
           '`NotFoundHttpException` (404)',
           'Ошибка PHP: too few arguments'],
  answer=1,
  why='Параметры действия заполняет `bindActionParams()`. Если обязательного параметра нет, он бросает '
      '`BadRequestHttpException` с сообщением «Missing required parameters» — тело действия не запускается.',
  ref=('controllers', 'Контроллеры и действия'), node=('controllers',)),

q(id='flow-action-case', topic='flow',
  text='В контроллере есть публичный метод `public function ActionIndex()` — с большой буквы. '
       'Будет ли он доступен по маршруту `site/index`?',
  options=['Да, имена методов в PHP регистронезависимы',
           'Нет — Yii дополнительно сверяет настоящее имя метода',
           'Да, но только при `YII_DEBUG`',
           'Да, с предупреждением в журнале'],
  answer=1,
  why='`method_exists()` в PHP действительно не различает регистр, поэтому Yii добавляет вторую проверку: '
      '`(new ReflectionMethod(...))->getName() === $methodName`. Она и отсекает `ActionIndex`.',
  ref=('controllers', 'Контроллеры и действия'), node=('controllers', 'traps')),

q(id='flow-before-order', topic='flow',
  text='В каком порядке вызываются `beforeAction()` приложения, модуля и контроллера?',
  options=['Контроллер → модуль → приложение',
           'Приложение → модуль → контроллер',
           'Модуль → контроллер → приложение',
           'Порядок не определён'],
  answer=1,
  why='Пре-фильтры идут снаружи внутрь, и любой `false` обрывает цепочку. Пост-фильтры — в обратном '
      'порядке: контроллер, модуль, приложение.',
  ref=('filters', 'Фильтры'), node=('filters',)),

q(id='flow-cors-order', topic='flow',
  text='Почему фильтр `Cors` объявляют **раньше** `authenticator`?',
  options=['`Cors` работает только первым в массиве `behaviors()`',
           'Браузер шлёт preflight `OPTIONS` без токена — аутентификация вернёт 401 раньше, чем уйдут CORS-заголовки',
           '`authenticator` удаляет заголовки, которые ставит `Cors`',
           'Порядок не важен, это вопрос стиля'],
  answer=1,
  why='Предварительный запрос `OPTIONS` браузер отправляет без авторизационных заголовков. Если первой '
      'отработает аутентификация, ответ будет 401 без нужных CORS-заголовков, и браузер не пустит основной запрос.',
  ref=('filters', 'Фильтры'), node=('filters', 'traps')),

q(id='flow-verbfilter', topic='flow',
  text='`VerbFilter` настроен как `[\'delete\' => [\'POST\', \'DELETE\']]`. Пришёл GET на `site/delete`. Что вернёт сервер?',
  options=['403 Forbidden', '404 Not Found',
           '405 Method Not Allowed и заголовок `Allow`', '400 Bad Request'],
  answer=2,
  why='`VerbFilter` бросает `MethodNotAllowedHttpException` и заодно проставляет заголовок `Allow` '
      'со списком разрешённых методов — как того требует HTTP.',
  ref=('filters', 'Фильтры'), node=('filters',)),

q(id='flow-acf-star', topic='flow',
  text='Чтобы пустить в действие вообще всех, в правило ACF написали `\'roles\' => [\'*\']`. Что получится?',
  options=['Пустит всех — это подстановочный знак',
           'Ничего не пустит: особых значений всего два, `?` и `@`, а `*` уйдёт в `$user->can(\'*\')`',
           'Ошибка конфигурации при старте',
           'Пустит только гостей'],
  answer=1,
  why='`AccessRule::matchRole()` отдельно разбирает только `?` (гость) и `@` (авторизованный). Всё остальное '
      'считается именем роли или разрешения и уходит в `can()`, который без настроенного RBAC вернёт `false`. '
      'Чтобы пустить всех, роли в правиле просто не указывают.',
  ref=('authorization', 'Авторизация и доступ'), node=('user', 'traps')),

# ------------------------------------------------------------------ представления

q(id='views-renderajax', topic='views',
  text='Чем `renderAjax()` отличается от `renderPartial()`?',
  options=['`renderAjax()` применяет шаблон, `renderPartial()` — нет',
           'Оба без шаблона, но `renderAjax()` ещё выводит зарегистрированные JS и CSS',
           '`renderAjax()` возвращает JSON',
           'Ничем, это псевдонимы'],
  answer=1,
  why='Шаблон не применяет ни тот, ни другой. Разница в ресурсах: фрагмент, подгружаемый по AJAX, '
      'обычно содержит виджеты со своими скриптами — без `renderAjax()` они бы просто потерялись.',
  ref=('views', 'Представления'), node=('views',)),

q(id='views-this', topic='views',
  text='Что такое `$this` внутри файла представления в Yii 2?',
  options=['Контроллер, как было в Yii 1',
           'Объект `yii\\web\\View`; контроллер доступен как `$this->context`',
           'Модель, переданную в `render()`',
           'Приложение `yii\\web\\Application`'],
  answer=1,
  why='Представление рендерит компонент `View`, он же и становится `$this`. Отсюда `$this->title`, '
      '`$this->registerJs()`, `$this->params`. Контроллер лежит в `$this->context`.',
  ref=('views', 'Представления'), node=('views', 'traps')),

q(id='views-double-slash', topic='views',
  text='Контроллер модуля `admin` вызывает `$this->render(\'//site/error\')`. Какой файл будет взят?',
  options=['`@app/modules/admin/views/site/error.php`',
           '`@app/views/site/error.php`',
           '`@app/views/admin/site/error.php`',
           'Зависит от `viewPath` модуля'],
  answer=1,
  why='Двойной слэш всегда означает папку представлений **приложения**, в обход модуля. Один слэш — '
      'от папки представлений текущего модуля, без слэша — от папки текущего контроллера.',
  ref=('views', 'Представления'), node=('views',)),

q(id='views-registerjs-key', topic='views',
  text='Зачем третий аргумент в `$this->registerJs($code, $position, $key)`?',
  options=['Задаёт порядок вывода блоков',
           'Это ключ блока: повторная регистрация с тем же ключом заменит прежний код, а не добавит второй',
           'Имя функции-обёртки',
           'Идентификатор пакета ресурсов'],
  answer=1,
  why='Без ключа им становится хеш самого кода, поэтому два разных вызова дадут два блока. Ключ нужен, '
      'когда один и тот же виджет рендерится несколько раз, а скрипт нужен один.',
  ref=('assets', 'Ресурсы'), node=('views',)),

q(id='views-sourcepath', topic='views',
  text='Когда пакету ресурсов нужен `sourcePath`, а когда `basePath` с `baseUrl`?',
  options=['`sourcePath` — для CSS, `basePath` — для JS',
           '`sourcePath` — когда файлы лежат вне веб-корня и их надо опубликовать; `basePath`/`baseUrl` — когда они уже в `web/`',
           '`sourcePath` — только для внешних CDN',
           'Это синонимы, оставлен один для совместимости'],
  answer=1,
  why='`sourcePath` включает публикацию: `AssetManager` копирует папку в `web/assets` под хешем. Для файлов, '
      'которые и так доступны по HTTP, публикация — лишняя работа, им достаточно `basePath` и `baseUrl`.',
  ref=('assets', 'Ресурсы'), node=('views',)),

q(id='views-html-a-escape', topic='views',
  text='Текст ссылки пришёл из базы и содержит теги. Что выведет `Html::a()`?',
  code=('php', r'''echo Html::a('<i>тык</i>', ['/post/view', 'id' => 7]);'''),   # ПРОВЕРЕНО запуском
  mono=True,
  options=['<a href="/post/7">&lt;i&gt;тык&lt;/i&gt;</a>',
           '<a href="/post/7"><i>тык</i></a>',
           'Бросит исключение: текст ссылки не должен содержать теги',
           '<a href="/post/view?id=7">тык</a>'],
  answer=1,
  why='`Html::a()` экранирует только значения атрибутов, а текст ссылки вставляет как есть — иначе в ссылку '
      'нельзя было бы положить иконку. Если текст пришёл от пользователя, оборачивайте его в `Html::encode()` сами.',
  ref=('security', 'Безопасность'), node=('helpers',)),

# ------------------------------------------------------------ модели и формы

q(id='model-bang-attr', topic='model',
  text='Что означает `!secret` в списке атрибутов сценария в `scenarios()`?',
  options=['Атрибут запрещён в этом сценарии',
           'Атрибут валидируется, но не присваивается массово — только явно',
           'Атрибут присваивается, но не валидируется',
           'Атрибут обязателен'],
  answer=1,
  why='Восклицательный знак делает атрибут активным, но небезопасным: правила на него работают, '
      'а `load()` его не заполнит. Так защищают поля вроде `is_admin`, которые задаются кодом.',
  ref=('models', 'Модели'), node=('scenarios',)),

q(id='model-load-false', topic='model',
  text='Форма `ContactForm`, в POST нет ключа `ContactForm`. Что вернёт `$model->load(Yii::$app->request->post())`?',
  options=['`true`, модель останется пустой', '`false`',
           '`null`', 'Бросит `InvalidArgumentException`'],
  answer=1,
  why='`load()` сам ищет данные по имени формы и возвращает `false`, если их нет. На этом и держится '
      'обычный шаблон действия: `if ($model->load(...) && $model->validate())` — иначе просто показываем форму.',
  ref=('forms', 'Формы'), node=('model',)),

q(id='model-unsafe-attr', topic='model',
  text='Атрибут есть в модели, поле было в POST, но после `load()` он пуст. Самая частая причина?',
  options=['Атрибут не объявлен в `attributes()`',
           'Атрибут не «безопасный»: для текущего сценария на него нет ни одного правила',
           'Не вызван `validate()`',
           'Имя поля в форме не совпало с именем атрибута'],
  answer=1,
  why='Массово присваиваются только безопасные атрибуты, а они по умолчанию выводятся из `rules()`. '
      'Нет правила — нет присваивания. Если проверять нечего, добавьте `[\'attr\', \'safe\']`.',
  ref=('forms', 'Формы'), node=('scenarios', 'traps')),

q(id='model-fields', topic='model',
  text='Модель Active Record отдаётся через REST. Что по умолчанию вернёт `fields()`?',
  options=['Только первичный ключ',
           'Все атрибуты записи, включая `password_hash`',
           'Ничего — метод надо переопределить',
           'Атрибуты, у которых есть правила в `rules()`'],
  answer=1,
  why='Базовая реализация возвращает все загруженные атрибуты — то есть всю строку таблицы. Для любой модели, '
      'которая выходит наружу, `fields()` нужно переопределять, а связи выносить в `extraFields()`.',
  ref=('rest', 'REST: основы'), node=('model',)),

q(id='model-scenario-ctor', topic='model',
  text='Как задать сценарий модели?',
  code=('php', r'''$model = new User(['scenario' => User::SCENARIO_LOGIN]);
// или
$model->scenario = User::SCENARIO_LOGIN;'''),
  options=['Только первым способом', 'Только вторым способом',
           'Обоими: `scenario` — обычное свойство, а конструктор применяет конфигурацию',
           'Ни одним: сценарий задаётся только в `scenarios()`'],
  answer=2,
  why='Конструктор `BaseObject` применяет переданный массив как конфигурацию, а `scenario` — свойство с '
      'геттером и сеттером. Поэтому работают оба варианта, разница только в удобстве.',
  ref=('models', 'Модели'), node=('scenarios',)),

q(id='model-loadmultiple', topic='model',
  text='Как назвать поля формы, чтобы `Model::loadMultiple()` разложил их по объектам?',
  options=['`$form->field($model, "attr[$index]")`',
           '`$form->field($model, "[$index]attr")`',
           '`$form->field($model, "attr")` с разными `id`',
           '`$form->field($model, "$index.attr")`'],
  answer=1,
  why='Индекс ставится **перед** именем атрибута: получится `Model[0][attr]`. Обратный порядок дал бы '
      '`Model[attr][0]` — это уже массив внутри одного атрибута, а не разные модели.',
  ref=('forms', 'Формы'), node=('model',)),

# ---------------------------------------------------------------------- валидация

q(id='valid-required-zero', topic='valid',
  text='Что выведет проверка?',
  code=('php', r'''$m = new DynamicModel(['qty' => 0]);
$m->addRule(['qty'], 'required');
var_dump($m->validate());'''),   # ПРОВЕРЕНО запуском
  options=['`bool(false)` — ноль считается пустым',
           '`bool(true)` — ноль не пустой',
           '`bool(false)`, но только в строгом режиме',
           'Бросит исключение'],
  answer=1,
  why='Пустыми Yii считает `null`, `\'\'` и `[]` — ноль и строка `\'0\'` в этот список не входят. Поэтому `required` '
      'пропускает ноль. Если по смыслу ноль недопустим, добавьте `[\'qty\', \'number\', \'min\' => 1]`.',
  ref=('validators', 'Встроенные валидаторы'), node=('rules', 'traps')),

q(id='valid-compare-dates', topic='valid',
  text='Правило `[\'end\', \'compare\', \'compareAttribute\' => \'start\', \'operator\' => \'>\']`, '
       'значения `end = \'2024-1-5\'`, `start = \'2024-01-10\'`. Пройдёт ли проверка?',   # ПРОВЕРЕНО запуском
  options=['Нет — 5 января раньше 10 января',
           'Да — строки сравниваются посимвольно, и `\'2024-1-5\'` оказывается «больше»',
           'Нет — Yii распознаёт даты сам',
           'Да, но только с `\'type\' => \'number\'`'],
  answer=1,
  why='По умолчанию оба значения приводятся к строке. На числах это обычно незаметно (PHP сравнивает числовые '
      'строки как числа, так что `\'10\' > \'9\'` — правда), а на датах в разном формате ломается. '
      '`\'type\' => \'number\'` тут тоже не спасёт: `(float)` от обеих дат даст `2024`. Сравнивайте UNIX-время из '
      '`timestampAttribute` валидатора `date`.',
  ref=('validators', 'Встроенные валидаторы'), node=('rules', 'traps')),

q(id='valid-integer-float-string', topic='valid',
  text='Пройдёт ли значение `\'5.0\'` правило `[\'a\', \'integer\']`?',   # ПРОВЕРЕНО запуском
  options=['Да — после приведения это целое', 'Нет',
           'Да, если не задан `strict`', 'Зависит от локали'],
  answer=1,
  why='`integer` — это `NumberValidator` с `integerOnly = true`, а он сверяет строку с шаблоном целого числа. '
      '`\'5.0\'` шаблону не соответствует, хотя численно и равно пяти.',
  ref=('validators', 'Встроенные валидаторы'), node=('rules',)),

q(id='valid-skiponempty', topic='valid', kind='many',
  text='У каких валидаторов `skipOnEmpty` выключен, то есть они срабатывают и на пустом значении? '
       'Отметьте все.',   # ПРОВЕРЕНО перебором builtInValidators
  options=['`required`', '`captcha`', '`email`', '`default`', '`filter`', '`trim`'],
  answer=[0, 1, 3, 4, 5],
  why='Таких пять. Двое из них проверяют — `required` и `captcha` (незаполненная капча должна сразу давать ошибку). '
      'Ещё трое ничего не проверяют, а меняют значение: `default` подставляет, `filter` и `trim` преобразуют — '
      'им пустое значение как раз и интересно.',
  ref=('validators', 'Встроенные валидаторы'), node=('rules',)),

q(id='valid-activeform-validate', topic='valid',
  text='Действие AJAX-валидации написано так. Что проверит этот вызов?',
  code=('php', r'''return ActiveForm::validate($model, 'email', 'username');'''),   # ПРОВЕРЕНО запуском
  options=['Оба атрибута', 'Только `email`', 'Только `username`',
           'Ничего — бросит ошибку о числе аргументов'],
  answer=1,
  why='Второй параметр — это **массив** имён атрибутов, а не список аргументов. Лишние аргументы учитываются '
      'только когда вторым идёт модель (так проверяют несколько моделей сразу). Правильно: '
      '`ActiveForm::validate($model, [\'email\', \'username\'])`.',
  ref=('validation', 'Валидация'), node=('rules',)),

q(id='valid-inline-client', topic='valid',
  text='Можно ли у inline-валидатора (метода модели) сделать проверку на стороне браузера?',
  options=['Нет, никогда — только серверная',
           'Да: в правиле задают `clientValidate`, и указанный метод возвращает JS-код',
           'Да, автоматически — Yii сам переводит PHP в JS',
           'Только через AJAX-валидацию'],
  answer=1,
  why='По умолчанию JS-кода для inline-валидатора действительно нет — взяться ему неоткуда. Но у '
      '`InlineValidator` есть свойство `clientValidate`: метод, который вернёт фрагмент JavaScript. '
      'Ещё варианты — вынести проверку в класс-валидатор или включить AJAX-валидацию.',
  ref=('validation', 'Валидация'), node=('rules',)),

# ------------------------------------------------------------------ Active Record

q(id='ar-save-false', topic='ar',
  text='`$post->save()` вернул `false`, исключения не было. Что случилось?',
  options=['Нет соединения с базой', 'Не прошла валидация — ошибки лежат в `$post->errors`',
           'Запись уже существует', 'Сработала оптимистичная блокировка'],
  answer=1,
  why='`save()` по умолчанию сначала валидирует и при ошибке молча возвращает `false`. Проблемы базы данных, '
      'наоборот, приходят исключением. Если валидация уже сделана раньше — `save(false)`.',
  ref=('active-record', 'Active Record'), node=('ar',)),

q(id='ar-updateall-events', topic='ar',
  text='К модели подключён `TimestampBehavior`. Обновит ли `Post::updateAll([\'title\' => \'...\'])` '
       'колонку `updated_at`?',   # ПРОВЕРЕНО запуском
  options=['Да, поведение подключено к модели',
           'Нет — массовые операции не создают объектов и не вызывают событий',
           'Да, но только у уже загруженных в память записей',
           'Только если вызвать `refresh()`'],
  answer=1,
  why='`updateAll()` — это один SQL-запрос `UPDATE` мимо объектов. Ни события `beforeSave`, ни поведения, ни '
      'валидация не срабатывают. Это плата за скорость и одновременно частая причина «протухших» дат.',
  ref=('active-record', 'Active Record'), node=('ar', 'traps')),

q(id='ar-no-changes', topic='ar',
  text='Запись загрузили, ничего не меняли и вызвали `save()`. Что произойдёт?',   # ПРОВЕРЕНО запуском
  options=['`UPDATE` со всеми колонками', 'Вернёт `true`, но запроса к базе не будет',
           'Вернёт `false`', 'Бросит исключение «нечего сохранять»'],
  answer=1,
  why='В `UPDATE` попадают только «грязные» атрибуты — изменённые после загрузки. Если их нет, запрос не '
      'отправляется вовсе, а метод отчитывается об успехе.',
  ref=('active-record', 'Active Record'), node=('ar',)),

q(id='ar-findone-array', topic='ar',
  text='Почему `Customer::findOne(Yii::$app->request->get(\'id\'))` — опасный код?',
  options=['`findOne()` не экранирует значение',
           'В `id` может прийти массив, и он превратится в набор условий по другим колонкам',
           '`findOne()` всегда возвращает первую запись таблицы',
           'Метод устарел и удалён'],
  answer=1,
  why='`?id[status]=1&id[id]=1` придёт как массив, а массив `findOne()` трактует как условие `where`. '
      'С 2.0.15 часть таких случаев отсекается, но полагаться на это не стоит: приводите тип `(int)$id` '
      'или пишите условие явно — `findOne([\'id\' => $id])`.',
  ref=('security', 'Безопасность'), node=('ar', 'traps')),

q(id='ar-with-joinwith', topic='ar',
  text='Нужно выбрать заказы и отфильтровать их по колонке связанной таблицы `customer.status`. '
       'Что использовать?',
  options=['`with(\'customer\')` — он подтянет связь',
           '`joinWith(\'customer\')` — нужен настоящий JOIN',
           'Любой из двух, разницы нет',
           '`select()` с именем связи'],
  answer=1,
  why='`with()` делает **отдельный** запрос для связи, поэтому в `where` основного запроса её колонок ещё нет. '
      '`joinWith()` добавляет JOIN — по нему и можно фильтровать и сортировать. Помните про дубли строк '
      'при JOIN с `hasMany`.',
  ref=('active-record', 'Active Record'), node=('ar', 'traps')),

q(id='ar-optimistic', topic='ar',
  text='Для чего нужен метод `optimisticLock()`?',
  options=['Блокирует строку в базе на время транзакции',
           'Не даёт перезаписать чужие правки: при несовпадении версии `save()` бросит `StaleObjectException`',
           'Ставит блокировку на файл',
           'Включает очередь записи'],
  answer=1,
  why='Настоящей блокировки нет: в таблице держат колонку версии, форма отдаёт её скрытым полем, а `UPDATE` '
      'идёт с условием по версии. Никто не мешал — версия совпала; кто-то успел раньше — исключение.',
  ref=('active-record', 'Active Record'), node=('ar',)),

# ------------------------------------------------------------ Query Builder и DAO

q(id='query-one-limit', topic='query',
  text='Какой SQL уйдёт в базу?',
  code=('php', r'''$row = (new Query())->from('post')->one();'''),   # ПРОВЕРЕНО: SQL снят с соединения
  mono=True,
  options=['SELECT * FROM `post` LIMIT 1',
           'SELECT * FROM `post`',
           'SELECT * FROM `post` LIMIT 0, 1',
           'SELECT TOP 1 * FROM `post`'],
  answer=1,
  why='`one()` не трогает запрос: он выполняет его как есть и забирает из результата первую строку через '
      '`fetch()`. На большой выборке базе придётся посчитать всё — ставьте `limit(1)` сами.',
  ref=('query-builder', 'Query Builder'), node=('query', 'traps')),

q(id='query-empty-in', topic='query',
  text='Что получится из условия с пустым массивом?',
  code=('php', r'''$sql = (new Query())->from('post')->where(['id' => []])->createCommand()->getRawSql();'''),   # ПРОВЕРЕНО запуском
  mono=True,
  options=['SELECT * FROM `post`',
           'SELECT * FROM `post` WHERE `id` IN ()',
           'SELECT * FROM `post` WHERE 0=1',
           'Бросит исключение'],
  answer=2,
  why='`IN ()` — некорректный SQL, поэтому Yii подставляет заведомо ложное условие: выборка честно вернёт '
      'ноль строк. Логично, но неожиданно — если пустой массив у вас означает «без фильтра», проверяйте его '
      'до добавления условия или берите `filterWhere()`.',
  ref=('query-builder', 'Query Builder'), node=('query', 'traps')),

q(id='query-filterwhere-zero', topic='query',
  text='Останется ли условие в запросе?',
  code=('php', r'''$q = (new Query())->from('post')->andFilterWhere(['status' => 0]);'''),   # ПРОВЕРЕНО запуском
  options=['Нет — ноль считается пустым и условие отбросится',
           'Да — условие `status = 0` останется в запросе',
           'Нет — `andFilterWhere` работает только со строками',
           'Да, но значение подставится как NULL'],
  answer=1,
  why='Пустыми для `filterWhere()` считаются `null`, `\'\'`, строка из одних пробелов и `[]`. Ноль и `\'0\'` — '
      'нормальные значения, условие остаётся. Именно поэтому фильтр «статус = 0» в поиске работает, '
      'а «строка пробелов» — нет.',
  ref=('query-builder', 'Query Builder'), node=('query',)),

q(id='query-count-limit', topic='query',
  text='Сколько вернёт `count()` при таком запросе, если условию отвечают 42 строки?',
  code=('php', r'''$n = (new Query())->from('post')->where(['status' => 1])->limit(10)->count();'''),   # ПРОВЕРЕНО запуском
  options=['10', '42', '1', 'Зависит от СУБД'],
  answer=1,
  why='Агрегаты специально отбрасывают `limit`, `offset` и `orderBy`, но сохраняют `where`, `join` и `groupBy`. '
      'Иначе постраничная навигация не могла бы узнать общее число строк, пользуясь тем же запросом.',
  ref=('query-builder', 'Query Builder'), node=('query', 'traps')),

q(id='query-braces', topic='query',
  text='Во что превратится `{{%post}}` и `[[title]]` при префиксе таблиц `tbl_`?',   # ПРОВЕРЕНО запуском
  options=['В `%post` и `title` — проценты и скобки просто отбрасываются',
           'В имя с префиксом и кавычками СУБД: `tbl_post` и экранированное имя столбца',
           'В `post` и `[title]` — скобки остаются как есть',
           'Ни во что: это просто текст в запросе'],
  answer=1,
  why='Двойные фигурные скобки — имя таблицы, процент внутри них — место для `tablePrefix`. Двойные квадратные — '
      'имя колонки. И то и другое экранируется под текущую СУБД, поэтому один запрос работает и в MySQL, '
      'и в PostgreSQL.',
  ref=('dao', 'DAO: прямые запросы'), node=('query',)),

q(id='query-transaction-master', topic='query',
  text='Настроена репликация «мастер — реплики». Куда уйдёт `SELECT`, выполненный внутри транзакции?',
  options=['На реплику — это чтение', 'На мастер',
           'Случайно на любую из реплик', 'Транзакция запретит чтение'],
  answer=1,
  why='`Command::prepare()` прямо проверяет: если транзакция открыта, чтение тоже идёт по её соединению. '
      'Иначе можно было бы не увидеть собственных, ещё не зафиксированных изменений.',
  ref=('dao', 'DAO: прямые запросы'), node=('query',)),

# ---------------------------------------------------------------------- миграции

q(id='migr-safeup', topic='migr',
  text='Чем `safeUp()` отличается от `up()`?',
  options=['`safeUp()` проверяет наличие таблиц перед изменением',
           '`safeUp()` выполняется в транзакции — при исключении изменения откатятся',
           '`safeUp()` нельзя откатить',
           'Это псевдонимы'],
  answer=1,
  why='Транзакцию открывает сама миграция. Оговорка одна: транзакционный DDL умеют не все СУБД — в MySQL '
      'до 8.0 `CREATE TABLE` откатить нельзя, и «безопасность» останется на бумаге.',
  ref=('migrations', 'Миграции'), node=('migrations',)),

q(id='migr-down-false', topic='migr',
  text='Когда `down()` должен возвращать `false`?',
  options=['Когда откат не нужен',
           'Когда откат невозможен — например, данные уничтожены',
           'Когда миграция уже применена',
           '`down()` ничего не возвращает'],
  answer=1,
  why='`false` — это сигнал «отката нет». `migrate/down` остановится и скажет об этом вместо того, чтобы '
      'молча сделать вид, будто всё вернулось на место.',
  ref=('migrations', 'Миграции'), node=('migrations',)),

q(id='migr-name', topic='migr',
  text='Что даст команда `./yii migrate/create create_post_table`?',
  options=['Пустую миграцию — имя ни на что не влияет',
           'Заготовку с `createTable(\'post\')` и `dropTable()` в откате',
           'Сразу создаст таблицу',
           'Ошибку: имя занято'],
  answer=1,
  why='Генератор разбирает имя: `create_xxx_table`, `drop_xxx_table`, `add_xxx_column_to_yyy_table` и '
      '`create_junction_table_for_xxx_and_yyy_tables`. С `--fields` в заготовку попадут и колонки.',
  ref=('migrations', 'Миграции'), node=('migrations',)),

q(id='migr-path', topic='migr',
  text='Как применить миграции расширения — например, таблицы RBAC?',
  options=['Скопировать файлы в `@app/migrations`',
           '`./yii migrate --migrationPath=@yii/rbac/migrations`',
           'Они применяются сами при установке через Composer',
           'Только вручную через SQL'],
  answer=1,
  why='Путь можно передать разово флагом или прописать в конфигурации консольного приложения — тогда команда '
      'будет видеть несколько источников сразу (`migrationPath` принимает массив).',
  ref=('migrations', 'Миграции'), node=('migrations',)),

q(id='migr-history', topic='migr',
  text='Откуда `./yii migrate` знает, какие миграции уже применены?',
  options=['Из имён файлов и их дат',
           'Из служебной таблицы `migration` в той же базе',
           'Из файла в `@runtime`',
           'Сравнивает схему базы с кодом'],
  answer=1,
  why='Таблица `migration` создаётся при первом запуске и хранит имя миграции и время применения. Поэтому '
      'откатить базу мимо Yii и ожидать, что команда это заметит, не получится.',
  ref=('migrations', 'Миграции'), node=('migrations',)),

q(id='migr-pk', topic='migr',
  text='Какой метод в миграции даёт автоинкрементный первичный ключ?',
  options=['`$this->integer()->autoIncrement()`', '`$this->primaryKey()`',
           '`$this->pk()`', '`$this->serial()`'],
  answer=1,
  why='`primaryKey()` разворачивается в подходящий для СУБД тип: `int AUTO_INCREMENT PRIMARY KEY` в MySQL, '
      '`serial PRIMARY KEY` в PostgreSQL. Для больших таблиц есть `bigPrimaryKey()`.',
  ref=('migrations', 'Миграции'), node=('migrations',)),

# --------------------------------------------------------- компоненты и события

q(id='core-readonly', topic='core',
  text='В классе есть только `getLabel()`, сеттера нет. Что будет при `$obj->label = \'x\'`?',
  options=['Создастся обычное публичное свойство', '`InvalidCallException` — свойство только для чтения',
           '`UnknownPropertyException`', 'Значение молча потеряется'],
  answer=1,
  why='`__set()` различает два случая: есть геттер, но нет сеттера — `InvalidCallException` («только для '
      'чтения»); нет ни того ни другого — `UnknownPropertyException`. Разные ошибки для разных ситуаций.',
  ref=('components', 'Компоненты и свойства'), node=('components',)),

q(id='core-init-order', topic='core',
  text='Почему `parent::__construct($config)` вызывают в конце своего конструктора, '
       'а `parent::init()` — в начале своего `init()`?',
  options=['Так требует PSR',
           'Конструктор родителя применяет конфигурацию и вызывает `init()` — свои умолчания надо задать до этого',
           'Иначе свойства станут приватными',
           'Порядок не важен'],
  answer=1,
  why='`BaseObject::__construct()` сначала раскладывает конфигурацию по свойствам, затем зовёт `init()`. '
      'Если вызвать родителя первым, ваши умолчания затрут значения из конфигурации. В `init()` наоборот: '
      'сначала пусть отработает родитель, а потом уже ваш код с готовым объектом.',
  ref=('components', 'Компоненты и свойства'), node=('components', 'traps')),

q(id='core-event-order', topic='core',
  text='На событие подписаны и конкретный объект (`$post->on(...)`), и класс (`Event::on(Post::class, ...)`). '
       'Кто сработает первым?',
  options=['Обработчик класса', 'Обработчик экземпляра',
           'В порядке подписки', 'Одновременно, порядок не определён'],
  answer=1,
  why='`Component::trigger()` сначала перебирает обработчики самого объекта и только потом вызывает '
      '`Event::trigger()` для уровня класса. И да: `$event->handled = true` в обработчике экземпляра '
      'до классовых просто не дойдёт.',
  ref=('events', 'События'), node=('events',)),

q(id='core-handled', topic='core',
  text='Как остановить выполнение остальных обработчиков события?',
  options=['`return false` из обработчика', '`$event->handled = true`',
           '`$event->stopPropagation()`', 'Бросить исключение — других способов нет'],
  answer=1,
  why='Возвращаемое значение обработчика вообще не смотрят. Флаг `handled` проверяется после каждого вызова, '
      'и цикл прерывается. У событий-«вето» вроде `beforeSave` для отмены действия есть отдельный флаг `isValid`.',
  ref=('events', 'События'), node=('events',)),

q(id='core-as-on', topic='core',
  text='Что означают ключи `on` и `as` в массиве конфигурации компонента?',
  options=['`on` — включить, `as` — переименовать',
           '`\'on событие\' => обработчик` подписывает на событие, `\'as имя\' => конфиг` прикрепляет поведение',
           '`on` — условие, `as` — псевдоним класса',
           'Это зарезервированные имена свойств'],
  answer=1,
  why='Их разбирает `Yii::configure()` до присваивания остальных свойств. Так и событие, и поведение можно '
      'навесить прямо в конфигурации, не трогая код класса.',
  ref=('configurations', 'Конфигурации'), node=('behaviors',)),

q(id='core-baseobject', topic='core',
  text='Когда достаточно унаследоваться от `BaseObject`, а когда нужен `Component`?',
  options=['`BaseObject` — для моделей, `Component` — для контроллеров',
           '`BaseObject` даёт свойства и конфигурацию; `Component` добавляет события и поведения',
           '`BaseObject` устарел',
           'Разница только в пространстве имён'],
  answer=1,
  why='`Component` наследует `BaseObject` и добавляет `on()`, `trigger()`, `attachBehavior()`. Это удобно, но '
      'не бесплатно: у каждого компонента есть внутренние массивы обработчиков и поведений. Для простого '
      'значения-объекта хватит `BaseObject`.',
  ref=('components', 'Компоненты и свойства'), node=('components',)),

# ------------------------------------------------------------- кэш, сессии, куки

q(id='state-session-nested', topic='state',
  text='Почему такая запись не сохранится в сессии?',
  code=('php', r'''Yii::$app->session['cart']['qty'] = 2;'''),
  options=['Нужно сначала вызвать `open()`',
           '`ArrayAccess` возвращает копию массива — меняется копия, а не то, что в сессии',
           'Ключ `cart` не объявлен заранее',
           'Сессия доступна только на чтение'],
  answer=1,
  why='`offsetGet()` отдаёт значение, а не ссылку, поэтому запись уходит во временную копию — PHP даже '
      'предупреждения не даст. Варианты: записать массив целиком, работать с `$_SESSION` напрямую после '
      '`open()` или хранить плоские ключи вроде `cart.qty`.',
  ref=('sessions', 'Сессии и куки'), node=('state', 'traps')),

q(id='state-flash', topic='state',
  text='Сколько живёт flash-сообщение?',
  options=['До конца текущего запроса',
           'Устанавливается в одном запросе, читается в следующем, затем удаляется',
           'До явного вызова `removeFlash()`',
           'Пока не истечёт время сессии'],
  answer=1,
  why='Ровно на один переход — поэтому это и работает для сообщений после редиректа. Если прочитать flash '
      'в том же запросе, где его поставили, он всё равно исчезнет к следующему.',
  ref=('sessions', 'Сессии и куки'), node=('state',)),

q(id='state-cache-false', topic='state',
  text='Что вернёт `$cache->get($key)`, если значения в кэше нет?',   # ПРОВЕРЕНО запуском
  options=['`null`', '`false`', 'Пустую строку', 'Бросит исключение'],
  answer=1,
  why='Промах — это `false`. Отсюда важное следствие: само значение `false` в кэш класть нельзя, вы не отличите '
      'его от промаха. Заворачивайте в массив или пользуйтесь `getOrSet()`.',
  ref=('caching', 'Кэширование данных'), node=('state', 'traps')),

q(id='state-tagdependency', topic='state',
  text='Как одним движением сбросить кэш всех списков постов после сохранения одного поста?',
  options=['`$cache->flush()`',
           'Записывать с `TagDependency([\'tags\' => \'posts\'])` и вызывать `TagDependency::invalidate($cache, \'posts\')`',
           'Удалять ключи по маске',
           'Задать всем записям `duration = 1`'],
  answer=1,
  why='`flush()` снесёт вообще весь кэш, включая чужой. Маски по ключам большинство хранилищ не умеют. '
      'Метки — штатный способ: помеченные записи разом становятся недействительными.',
  ref=('caching', 'Кэширование данных'), node=('state',)),

q(id='state-cookie-tamper', topic='state',
  text='Включена проверка кук. Пользователь поправил значение куки в браузере. Что увидит приложение?',
  options=['Изменённое значение',
           '`Yii::$app->request->cookies` эту куку не вернёт, хотя в `$_COOKIE` она есть',
           'Исключение при старте',
           'Пустую строку вместо значения'],
  answer=1,
  why='К значению приписывается подпись на `cookieValidationKey`. Не сошлась — кука просто не попадает в '
      'коллекцию. Именно поэтому читать `$_COOKIE` напрямую в Yii-приложении — плохая привычка.',
  ref=('sessions', 'Сессии и куки'), node=('state',)),

q(id='state-keyprefix', topic='state',
  text='Зачем компоненту кэша свойство `keyPrefix`?',
  options=['Чтобы ключи были короче',
           'Чтобы несколько приложений на одном memcached, Redis или APCu не затирали ключи друг друга',
           'Для сортировки ключей',
           'Это префикс таблицы для `DbCache`'],
  answer=1,
  why='Общие хранилища не разделены по приложениям: два проекта с ключом `user-1` на одном сервере будут '
      'видеть чужие данные. Префикс разводит их по пространствам имён.',
  ref=('caching', 'Кэширование данных'), node=('state',)),

# ------------------------------------------------------- безопасность и доступ

q(id='sec-password', topic='sec',
  text='Как правильно хранить пароль пользователя?',
  options=['`md5($password)` с солью',
           '`Yii::$app->security->generatePasswordHash($password)`, проверять `validatePassword()`',
           '`sha256` с `keyPrefix`',
           'Шифровать `encryptByKey()` и расшифровывать при входе'],
  answer=1,
  why='Под капотом bcrypt: соль генерируется сама и хранится внутри хеша, а стоимость подбирается так, чтобы '
      'перебор был дорогим. Шифрование тут в принципе неуместно — пароль не нужно уметь расшифровывать.',
  ref=('security', 'Безопасность'), node=('user',)),

q(id='sec-encode', topic='sec',
  text='Что именно экранирует `Html::encode()` — только угловые скобки или кавычки тоже?',
  code=('php', r'''echo Html::encode('<b>"A" & \'B\'</b>');'''),   # ПРОВЕРЕНО запуском
  mono=True,
  options=['&lt;b&gt;"A" &amp; \'B\'&lt;/b&gt; — кавычки не трогаются',
           '&lt;b&gt;&quot;A&quot; &amp; &#039;B&#039;&lt;/b&gt; — экранируются и кавычки тоже',
           '&lt;b&gt;&quot;A&quot; & \'B\'&lt;/b&gt;',
           'b"A" &amp; \'B\'/b — теги вырезаются'],
  answer=1,
  why='`Html::encode()` — это `htmlspecialchars()` с `ENT_QUOTES`, поэтому экранируются и одинарные, и двойные '
      'кавычки. Значит, результат безопасно подставлять и в текст, и в значение атрибута.',
  ref=('security', 'Безопасность'), node=('helpers',)),

q(id='sec-csrf-off', topic='sec',
  text='Как отключить проверку CSRF для одного действия-вебхука?',
  options=['`Yii::$app->request->enableCsrfValidation = false` в самом действии',
           'В `beforeAction()` контроллера выставить `$this->enableCsrfValidation = false` для нужного `$action->id`',
           'Удалить `cookieValidationKey`',
           'Отключить CSRF для всего приложения'],
  answer=1,
  why='Проверка происходит до входа в действие, поэтому внутри него отключать уже поздно. Свойство ставят в '
      '`beforeAction()` — до `parent::beforeAction($action)` — и только для конкретного действия.',
  ref=('security', 'Безопасность'), node=('http',)),

q(id='sec-encrypt', topic='sec',
  text='Чем `encryptByKey()` отличается от `encryptByPassword()`?',
  options=['Первый быстрее, но требует случайного ключа достаточной длины; второй растягивает пароль через PBKDF2 и потому медленнее',
           'Первый симметричный, второй асимметричный',
           'Первый устарел',
           'Разница только в длине результата'],
  answer=0,
  why='Ключ — это уже криптостойкая случайная строка, из неё можно выводить подключи быстро. Человеческий '
      'пароль энтропии почти не содержит, поэтому его намеренно растягивают — иначе перебор был бы дешёвым.',
  ref=('security', 'Безопасность'), node=('user',)),

q(id='sec-loginrequired', topic='sec',
  text='AJAX-запрос от гостя попал на действие, закрытое `AccessControl`. Почему вместо редиректа '
       'на страницу входа пришла ошибка 403?',
  options=['Для AJAX редиректы запрещены спецификацией',
           'Yii смотрит заголовок `Accept`: если клиент не готов принять HTML, редиректить некуда — будет 403',
           'Сработал `VerbFilter`',
           'Не задан `loginUrl`'],
  answer=1,
  why='Решает `checkRedirectAcceptable()`, а не флаг AJAX. Запрос, который просит `application/json`, отправлять '
      'на HTML-страницу входа бессмысленно, поэтому `loginRequired()` бросает `ForbiddenHttpException`.',
  ref=('authentication', 'Аутентификация'), node=('user', 'traps')),

q(id='sec-order-by', topic='sec',
  text='Имя колонки для `ORDER BY` пришло от пользователя. Как подставить его безопасно?',
  options=['Экранировать через `Yii::$app->db->quoteValue()`',
           'Привязать параметром `:col`',
           'Сверить со списком допустимых колонок и обернуть в `[[...]]`',
           'Достаточно `addslashes()`'],
  answer=2,
  why='Параметрами привязывают значения, а не идентификаторы — `ORDER BY :col` превратится в сортировку по '
      'строковой константе. Единственный надёжный путь — белый список. Для сортировки в списках это уже '
      'сделано за вас в `yii\\data\\Sort`.',
  ref=('dao', 'DAO: прямые запросы'), node=('query', 'traps')),

# ------------------------------------------------------ консоль, i18n, REST, прочее

q(id='misc-exitcode', topic='api',
  text='Что должно вернуть консольное действие, чтобы cron счёл запуск неудачным?',
  options=['`false`', 'Ненулевой код — например, `ExitCode::UNSPECIFIED_ERROR`',
           'Пустую строку', 'Бросить исключение — других способов нет'],
  answer=1,
  why='Код возврата действия становится кодом выхода процесса. `ExitCode::OK` — это ноль, '
      '`UNSPECIFIED_ERROR` — единица; в классе есть и другие значения из sysexits.h.',
  ref=('console', 'Консольные команды'), node=('controllers',)),

q(id='misc-interactive', topic='api',
  text='Команда просит подтверждения, а её надо запускать из CI. Что сделать?',
  options=['Передать `--interactive=0`', 'Перенаправить `yes |` в неё',
           'Выставить `YII_ENV=prod`', 'Переопределить `confirm()`'],
  answer=0,
  why='`Controller::confirm()` при `interactive = 0` просто возвращает `true`, ничего не спрашивая. Опция '
      'объявлена в базовом консольном контроллере, поэтому работает у любой команды.',
  ref=('console', 'Консольные команды'), node=('controllers',)),

q(id='misc-array-arg', topic='api',
  text='Как передать команде массив значений одним аргументом?',
  code=('php', r'''public function actionSend(array $ids) { /* ... */ }'''),
  options=['`./yii send --ids=1 --ids=2`', '`./yii send 1,2,3` — Yii разобьёт строку по запятым',
           '`./yii send "[1,2,3]"` — разберётся как JSON', 'Массивы в аргументах не поддерживаются'],
  answer=1,
  why='`bindActionParams()` смотрит на тип параметра: если это `array`, строка режется по запятым '
      '(с обрезкой пробелов). Пустая строка даёт пустой массив.',
  ref=('console', 'Консольные команды'), node=('controllers',)),

q(id='misc-t-missing', topic='world',
  text='Что вернёт `Yii::t(\'app\', \'Hello\')`, если перевода нет?',
  options=['Пустую строку', 'Исходную строку `Hello` и вызовет событие `missingTranslation`',
           '`null`', 'Ключ в виде `app.Hello`'],
  answer=1,
  why='Пропущенный перевод не должен ломать страницу, поэтому возвращается исходный текст. А событие '
      '`missingTranslation` у источника сообщений позволяет их собирать — например, дописывать в файл на лету.',
  ref=('i18n', 'Интернационализация'), node=('app',)),

q(id='misc-rest-422', topic='api',
  text='REST-действие вызвало `save()`, валидация не прошла. Что уйдёт клиенту?',
  options=['200 с полем `errors`', '400 и текст первой ошибки',
           '422 и массив вида `[{"field": "...", "message": "..."}]`', '500'],
  answer=2,
  why='Ошибки модели сериализует `yii\\rest\\Serializer`: он ставит 422 «Data Validation Failed» и раскладывает '
      'первые ошибки каждого атрибута в плоский список. Удобно показывать прямо рядом с полями формы.',
  ref=('rest-advanced', 'REST: продолжение'), node=('controllers',)),

q(id='misc-schema-cache', topic='tools',
  text='Какая настройка компонента `db` сильнее прочих влияет на скорость Active Record в production?',
  options=['`enableQueryCache`', '`enableSchemaCache`',
           '`emulatePrepare`', '`charset`'],
  answer=1,
  why='Без кэша схемы Yii перед работой с таблицей каждый раз спрашивает у базы список колонок и их типы — '
      'лишний запрос на ровном месте, причём на каждый HTTP-запрос. В разработке его держат выключенным, '
      'иначе миграции «не видны».',
  ref=('performance', 'Производительность'), node=('ar',)),

# ============================================================ старт и устройство

q(id='start-name', topic='start',
  text='Что означает название Yii и как оно читается?',
  options=['«Уай-ай-ай»; сокращение от Yes I Insist',
           '«Йи» (`[ji:]`); в китайском — «простой и эволюционирующий», плюс акроним Yes It Is',
           '«Игрек-два»; это просто номер версии',
           '«Ии»; по фамилии автора'],
  answer=1,
  why='Первое, что стоит уметь произносить вслух. Китайское значение — «простой и эволюционирующий», '
      'а шуточная расшифровка Yes It Is прижилась в самом руководстве.',
  ref=('intro', 'Что такое Yii 2')),

q(id='start-2vs1', topic='start',
  text='Чем ветка 2.0 принципиально отличается от 1.1?',
  options=['Только переименованием классов',
           'Переписана с нуля: Composer, PSR, пространства имён, трейты',
           'Это та же кодовая база с новым шаблоном оформления',
           '2.0 — форк сообщества, не связанный с командой Yii'],
  answer=1,
  why='Прямого пути обновления нет именно поэтому: 1.1 живёт своей жизнью в режиме поддержки, '
      'а 2.0 построена на инструментах, которых во времена 1.1 просто не было.',
  ref=('intro', 'Что такое Yii 2')),

q(id='start-prereq', topic='start',
  text='Что нужно знать до начала работы с Yii 2?',
  options=['Только базовый синтаксис PHP',
           'ООП в PHP с пространствами имён и трейтами, Composer и командную строку',
           'JavaScript и сборщики фронтенда',
           'SQL и администрирование сервера баз данных'],
  answer=1,
  why='Composer тут не «одна из возможностей», а способ установки самого фреймворка и всех расширений; '
      'пространства имён лежат в основе автозагрузки; консоль нужна для миграций и генераторов.',
  ref=('intro', 'Что такое Yii 2')),

q(id='start-webroot', topic='start',
  text='Почему корнем веб-сервера делают `basic/web`, а не `basic`?',
  options=['Так короче путь к статике',
           'Наружу должны смотреть только входной скрипт и статика: конфиги, код, `runtime/` и `vendor/` остаются недоступными по HTTP',
           'Иначе не заработает `.htaccess`',
           'Этого требует Composer'],
  answer=1,
  why='Если корнем сделать папку проекта, по прямому адресу станут доступны `config/db.php` с паролем, '
      'логи из `runtime/` и исходники из `vendor/`. Ошибка старая и дорогая.',
  ref=('install', 'Установка и запуск')),

q(id='start-advanced', topic='start',
  text='Чем шаблон advanced отличается от basic?',
  options=['В нём больше расширений «из коробки»',
           'Три приложения (frontend, backend, console) с общим `common`, механизм окружений `php init` и готовые регистрация с восстановлением пароля',
           'Он использует другую версию фреймворка',
           'В нём нет консольного приложения'],
  answer=1,
  why='Разница не в количестве кода, а в структуре: отдельные точки входа для публичной части и админки, '
      'общий слой моделей и заготовки окружений под dev и prod.',
  ref=('install', 'Установка и запуск')),

q(id='start-requirements', topic='start',
  text='Как одной командой проверить, подходит ли сервер под требования Yii?',
  options=['`./yii serve --check`', '`php requirements.php` в корне проекта',
           '`composer diagnose`', '`./yii help`'],
  answer=1,
  why='Скрипт приходит вместе с шаблоном приложения и проверяет версию PHP и нужные расширения. '
      'Его же можно открыть браузером — тогда видно, чего не хватает именно веб-серверу, а не CLI.',
  ref=('install', 'Установка и запуск')),

q(id='start-route-to-class', topic='start',
  text='Во что превратится маршрут `post-comment/create-reply`?',
  options=['`app\\controllers\\PostComment::createReply()`',
           '`app\\controllers\\PostCommentController::actionCreateReply()`',
           '`app\\controllers\\post_comment\\CreateReply()`',
           '`app\\PostCommentController::createReply()`'],
  answer=1,
  why='Дефисы убираются, слова капитализируются, к имени класса добавляется суффикс `Controller`, '
      'к имени метода — префикс `action`. Это же правило работает и в обратную сторону, когда вы строите ссылку.',
  ref=('first-app', 'Первое приложение'), node=('controllers',)),

q(id='start-ar-minimum', topic='start',
  text='Что нужно, чтобы заработал класс `class Country extends ActiveRecord {}` без единой строки внутри?',
  options=['Объявить все атрибуты как публичные свойства',
           'Настроенное подключение `db` и таблица `country` в базе — остальное читается из схемы',
           'Описать `rules()` и `attributeLabels()`',
           'Сгенерировать класс через Gii, вручную не заработает'],
  answer=1,
  why='Имя таблицы выводится из имени класса, а список атрибутов и их типы — из схемы базы. '
      'Правила и подписи добавляют, когда они нужны, а не чтобы «завести» модель.',
  ref=('first-app', 'Первое приложение'), node=('ar',)),

q(id='start-gii-ip', topic='start',
  text='Почему Gii по умолчанию открывается только с локального адреса?',
  options=['Из-за ограничений PHP',
           'Из соображений безопасности: генератор пишет файлы на диск, поэтому список адресов задаётся в `allowedIPs`',
           'Потому что он работает только со встроенным сервером',
           'Это можно отключить только пересборкой расширения'],
  answer=1,
  why='Открытый наружу генератор кода — это удалённая запись файлов на сервер. Поэтому доступ ограничен '
      'явным списком адресов, а сам модуль подключают только в конфигурации для разработки.',
  ref=('first-app', 'Первое приложение')),

q(id='start-required-config', topic='start',
  text='Какие два свойства обязательны в конфигурации приложения?',
  options=['`name` и `language`', '`id` и `basePath`',
           '`basePath` и `components`', '`id` и `bootstrap`'],
  answer=1,
  why='`id` различает приложения между собой, а `basePath` задаёт псевдоним `@app` — от него отсчитываются '
      '`runtimePath`, `viewPath` и всё остальное. Без них приложение не создастся.',
  ref=('app', 'Приложение и его компоненты'), node=('app',)),

q(id='start-lazy-components', topic='start',
  text='Когда создаётся компонент приложения, объявленный в `components`?',
  options=['При старте приложения, все сразу',
           'Лениво — при первом обращении `Yii::$app->id`',
           'При первом запросе к базе',
           'Только если он перечислен в `bootstrap`'],
  answer=1,
  why='Ленивое создание экономит работу: компонент почты не поднимется на странице, где письма не шлют. '
      'Если компонент должен существовать с самого начала (например, `log`), его ID добавляют в `bootstrap`.',
  ref=('app', 'Приложение и его компоненты'), node=('app',)),

q(id='start-catchall', topic='start',
  text='Как одной строкой конфигурации перевести сайт в режим обслуживания?',
  options=['`\'maintenance\' => true`',
           '`\'catchAll\' => [\'site/offline\']`',
           '`\'defaultRoute\' => \'site/offline\'`',
           '`\'layout\' => \'offline\'`'],
  answer=1,
  why='`catchAll` перехватывает **все** маршруты и уводит их в одно действие. `defaultRoute` так не работает: '
      'он задаёт маршрут только для пустого адреса, а `/post/view` по-прежнему дойдёт до контроллера.',
  ref=('app', 'Приложение и его компоненты'), node=('app',)),

q(id='start-bootstrap', topic='start',
  text='Что делает `bootstrap()` приложения?',
  options=['Создаёт объекты контроллера и действия',
           'Подключает расширения из `extensions.php`, вызывает их bootstrap-классы и поднимает компоненты и модули из свойства `bootstrap`',
           'Разбирает URL и выбирает маршрут',
           'Загружает конфигурацию из файла'],
  answer=1,
  why='Это точка, где чужой код успевает вклиниться до маршрутизации: расширение может зарегистрировать '
      'псевдоним, подписаться на событие или добавить правило URL — иначе было бы поздно.',
  ref=('lifecycle', 'Жизненный цикл запроса'), node=('app',)),

q(id='start-errorhandler-early', topic='start',
  text='Почему обработчик ошибок регистрируется до применения конфигурации?',
  options=['Чтобы успеть прочитать `YII_DEBUG`',
           'Чтобы ошибки в самой конфигурации и при создании компонентов уже перехватывались и показывались понятно',
           'Так требует PSR-3',
           'Чтобы записать в журнал время старта'],
  answer=1,
  why='Опечатка в конфигурации — это тоже ошибка, и без установленного обработчика вы увидели бы голый '
      'фатал PHP вместо страницы с объяснением.',
  ref=('lifecycle', 'Жизненный цикл запроса')),

q(id='start-autoload-order', topic='start',
  text='Где подключать сторонний автозагрузчик относительно `Yii.php`?',
  options=['После — иначе Yii его перезапишет',
           'До: тогда автозагрузчик Yii встанет в очередь первым и будет пробовать раньше остальных',
           'Порядок не важен',
           'Сторонние автозагрузчики с Yii несовместимы'],
  answer=1,
  why='`Yii.php` регистрирует свой автозагрузчик с `prepend = true`, то есть в начало очереди. Поэтому его '
      'подключают последним: во входном скрипте сначала идёт `vendor/autoload.php` Composer, потом `Yii.php`.',
  ref=('lifecycle', 'Жизненный цикл запроса')),

# ================================================ контроллеры, фильтры, модули

q(id='flow-action-class', topic='flow',
  text='Чем отдельный класс действия лучше метода `actionXxx()` в контроллере?',
  options=['Он работает быстрее',
           'Его можно переиспользовать в разных контроллерах, настраивать через конфигурацию и распространять в расширениях',
           'Только в нём доступен `$this->render()`',
           'Он не требует объявления в `actions()`'],
  answer=1,
  why='Именно так устроены `ErrorAction`, `CaptchaAction` и действия REST: класс живёт отдельно, '
      'подключается через `actions()` и настраивается массивом, как любой другой объект Yii.',
  ref=('controllers', 'Контроллеры и действия'), node=('controllers',)),

q(id='flow-module-default', topic='flow',
  text='Пришёл маршрут `forum` — только имя модуля, без контроллера и действия. Что выполнится?',
  options=['Ошибка 404',
           '`defaultRoute` модуля (по умолчанию `default`) и `defaultAction` контроллера, то есть `DefaultController::actionIndex()`',
           'Первое объявленное действие первого контроллера',
           '`SiteController::actionIndex()` приложения'],
  answer=1,
  why='У модуля свой `defaultRoute` со значением `default`, а у контроллера — свой `defaultAction` со значением '
      '`index`. Оба можно переопределить, и это обычный способ сделать у модуля «главную».',
  ref=('modules', 'Модули'), node=('app',)),

q(id='flow-module-rules', topic='flow',
  text='Куда модуль должен добавлять свои правила URL?',
  options=['В `init()` модуля',
           'В `bootstrap()` модуля, включённого в `bootstrap` приложения',
           'В `behaviors()` его контроллеров',
           'В `rules()` — такого метода у модуля нет, поэтому только в конфигурацию приложения'],
  answer=1,
  why='`init()` выполняется, когда модуль уже создан — а создаётся он при разборе маршрута, то есть после того, '
      'как `UrlManager` отработал. `bootstrap()` вызывается до маршрутизации, и правила успевают встать на место.',
  ref=('modules', 'Модули'), node=('routing', 'traps')),

q(id='flow-module-instance', topic='flow',
  text='Как из кода получить экземпляр модуля, не зная, под каким ID его подключили?',
  options=['`Yii::$app->modules[\'forum\']`',
           '`MyModule::getInstance()` или `Yii::$app->controller->module`',
           '`new MyModule()`',
           '`Yii::$container->get(MyModule::class)`'],
  answer=1,
  why='`getInstance()` возвращает тот экземпляр, который обслуживает текущий запрос, — с его конфигурацией '
      'и компонентами. Создавать модуль через `new` бессмысленно: это будет чужой, ненастроенный объект.',
  ref=('modules', 'Модули'), node=('app',)),

# ========================================== представления, ресурсы, виджеты

q(id='views-appendtimestamp', topic='views',
  text='Как заставить браузеры забрать новый `site.css` сразу после выкладки?',
  options=['Переименовывать файл вручную при каждом релизе',
           '`\'appendTimestamp\' => true` у `assetManager` — к адресу добавится `?v=<время изменения файла>`',
           'Поставить `Cache-Control: no-store` для всей статики',
           'Включить `forceCopy`'],
  answer=1,
  why='Метка времени берётся из `filemtime()`, поэтому меняется ровно тогда, когда меняется файл. '
      '`forceCopy` решает другую задачу — перепубликацию исходников при каждом запросе, и в проде вреден.',
  ref=('assets', 'Ресурсы и клиентские скрипты'), node=('views',)),

q(id='views-widget-vs-begin', topic='views',
  text='Чем `Widget::widget()` отличается от `Widget::begin()`?',
  options=['`widget()` возвращает объект, `begin()` — строку',
           '`widget()` сразу возвращает готовый HTML; `begin()` отдаёт объект и ждёт `end()`, а между ними можно выводить содержимое',
           '`begin()` работает только в шаблонах, `widget()` — в контроллерах',
           'Разницы нет, это псевдонимы'],
  answer=1,
  why='Парный вызов нужен виджетам, которые оборачивают чужую разметку: `ActiveForm`, `Pjax`, `Modal`. '
      'Всё, что вывелось между `begin()` и `end()`, попадает внутрь виджета.',
  ref=('widgets', 'Виджеты'), node=('widgets',)),

q(id='views-widget-views', topic='views',
  text='Где виджет по умолчанию ищет свои файлы представлений?',
  options=['В `@app/views/widgets`',
           'В папке `views/` рядом с файлом класса виджета',
           'В `@app/views` того контроллера, который его вывел',
           'Нигде: виджет обязан возвращать строку'],
  answer=1,
  why='`getViewPath()` строит путь от файла класса через рефлексию — поэтому виджет самодостаточен и '
      'переносится в расширение вместе со своими шаблонами. Путь можно переопределить.',
  ref=('widgets', 'Виджеты'), node=('widgets',)),

q(id='views-widget-scripts', topic='views',
  text='Куда виджет должен регистрировать свои скрипты?',
  options=['В `Yii::$app->view` — глобальный объект представления',
           'В `$this->view` — то представление, в котором виджет рендерится; лучше всего через пакет ресурсов',
           'Прямо в вывод, тегом `<script>`',
           'В `Yii::$app->assetManager`'],
  answer=1,
  why='`$this->view` — это представление-владелец, и именно оно соберёт скрипты в нужную позицию страницы. '
      'Тег `<script>` прямо в выводе сломает порядок зависимостей и объединение ресурсов командой `yii asset`.',
  ref=('widgets', 'Виджеты'), node=('widgets',)),

# ================================================= вывод данных: формат и списки

q(id='output-asdate-null', topic='output',
  text='Что вернёт `Yii::$app->formatter->asDate(null)`?',
  options=['Пустую строку', 'Значение `nullDisplay` — по умолчанию `(not set)`',
           'Текущую дату', 'Бросит исключение'],
  answer=1,
  why='Форматтер отдельно обрабатывает `null`, чтобы в таблицах не появлялось «1 января 1970». '
      'Заглушку меняют свойством `nullDisplay` — например, на прочерк.',
  ref=('formatting', 'Форматирование данных'), node=('helpers',)),

q(id='output-timezone', topic='output',
  text='`asDatetime(\'2014-01-01 00:00:00\')` показал час ночи вместо полуночи. Почему?',
  options=['Ошибка округления',
           'Значение считается заданным в `defaultTimeZone` и переводится в `timeZone` форматтера — например, из UTC в Europe/Berlin',
           'Строку не удалось разобрать, взято текущее время',
           'Не установлено расширение intl'],
  answer=1,
  why='Это два разных свойства: `defaultTimeZone` — в каком поясе лежат данные, `timeZone` — в каком их показывать. '
      'Пара настроена почти всегда, просто про неё забывают.',
  ref=('formatting', 'Форматирование данных'), node=('helpers',)),

q(id='output-no-intl', topic='output',
  text='Что будет с форматированием, если расширение intl не установлено?',
  options=['Приложение не запустится',
           'Форматирование работает, но без локализации: английские названия, US-разделители, нет `asSpellout()` и `asOrdinal()`',
           'Все методы вернут `nullDisplay`',
           'Разницы нет'],
  answer=1,
  why='Yii держит запасную реализацию на функциях PHP, поэтому страница не падает — но числа и даты '
      'выглядят по-американски. Из-за этого версии intl и ICU стоит держать одинаковыми на всех окружениях.',
  ref=('formatting', 'Форматирование данных')),

q(id='output-sqlprovider-count', topic='output',
  text='Почему `SqlDataProvider` требует указать `totalCount`, а `ActiveDataProvider` — нет?',
  options=['`SqlDataProvider` не умеет считать строки',
           'По объекту `Query` провайдер сам построит `COUNT(*)`, а произвольный SQL он переписать не может',
           '`ActiveDataProvider` всегда выбирает все строки и считает их в PHP',
           '`totalCount` нужен только для REST'],
  answer=1,
  why='Разобрать и переписать готовую строку SQL в счётный запрос нельзя надёжно, поэтому общее число '
      'приходится сообщать самому — обычно отдельным запросом.',
  ref=('data-providers', 'Провайдеры данных'), node=('query',)),

q(id='output-page-numbering', topic='output',
  text='С какого числа начинается нумерация страниц в объекте `Pagination` и в адресе?',
  options=['И там и там с 1', 'В объекте с 0, в параметре `page` — с 1',
           'В объекте с 1, в параметре `page` — с 0', 'И там и там с 0'],
  answer=1,
  why='Внутри удобно считать от нуля — из номера сразу получается `offset`. Снаружи человеку привычнее '
      'единица, поэтому `Pagination` переводит одно в другое сам. Отсюда и вечная путаница на единицу.',
  ref=('data-providers', 'Провайдеры данных')),

q(id='output-sort-param', topic='output',
  text='Что означает `?sort=-created_at,title` в адресе списка?',
  options=['Сортировку по `created_at` по убыванию, затем по `title` по возрастанию',
           'Сортировку только по `created_at`, остальное игнорируется',
           'Исключение `created_at` из сортировки',
           'Сортировку по `title` с фильтром по `created_at`'],
  answer=0,
  why='Минус означает убывание, запятая разделяет уровни. Второй и последующие уровни учитываются только '
      'при `enableMultiSort = true` — по умолчанию он выключен.',
  ref=('data-providers', 'Провайдеры данных')),

q(id='output-column-format', topic='output',
  text='Что означает строка колонки `\'created_at:datetime:Создано\'` в `GridView`?',
  options=['Три разных колонки через двоеточие',
           'Атрибут `created_at`, формат `datetime`, заголовок «Создано»',
           'Атрибут `created_at` с фильтром по типу `datetime`',
           'Атрибут, значение по умолчанию и подсказку'],
  answer=1,
  why='Краткая форма разбирается как `атрибут:формат:заголовок`, формат и заголовок необязательны. '
      'Когда нужно больше — колонку описывают массивом с ключами `attribute`, `format`, `label`, `value`.',
  ref=('data-widgets', 'Виджеты данных'), node=('widgets',)),

q(id='output-searchmodel', topic='output',
  text='Зачем поисковой модели (`PostSearch`) наследоваться от самой AR-модели?',
  options=['Иначе не заработает `GridView`',
           'Чтобы получить готовые атрибуты, подписи и типы; `rules()` переопределяются под фильтр, а `search()` строит провайдер',
           'Чтобы фильтры сохранялись в базе',
           'Это требование Gii, технически не нужно'],
  answer=1,
  why='Наследование даёт бесплатно `attributeLabels()` и список атрибутов, но правила нужны другие: '
      'в фильтре все поля необязательны и «безопасны», иначе `load()` их не примет.',
  ref=('data-widgets', 'Виджеты данных')),

q(id='output-pjax', topic='output',
  text='Что даёт обёртка `GridView` в виджет `Pjax`?',
  options=['Кэширование таблицы на сервере',
           'Пагинация, сортировка и фильтрация обновляют только таблицу, без перезагрузки страницы',
           'Защиту таблицы от CSRF',
           'Экспорт в CSV'],
  answer=1,
  why='Pjax перехватывает ссылки и отправку форм внутри себя, забирает ответ по AJAX и подменяет только '
      'свой кусок разметки. Адрес в строке браузера при этом меняется, поэтому «назад» работает.',
  ref=('data-widgets', 'Виджеты данных'), node=('widgets',)),

# ====================================== модели, валидация — добор до трёх

q(id='model-labels', topic='model',
  text='Откуда берётся подпись поля в форме, если `attributeLabels()` не объявлен?',
  options=['Из имени столбца как есть',
           'Имя атрибута разбивается по подчёркиваниям и camelCase и капитализируется: `created_at` → «Created At»',
           'Подписи не будет вовсе',
           'Из комментария к столбцу в базе'],
  answer=1,
  why='`generateAttributeLabel()` делает это через `Inflector::camel2words()`. Для русского интерфейса '
      'подписи всё равно приходится задавать руками, но пустого поля не будет никогда.',
  ref=('models', 'Модели'), node=('model',)),

q(id='valid-dynamicmodel', topic='valid',
  text='Как проверить несколько значений правилами, не заводя класс модели?',
  options=['`Validator::validateAll($data, $rules)`',
           '`DynamicModel::validateData($data, $rules)` или `new DynamicModel()` с `addRule()`',
           'Никак — правила работают только внутри модели',
           '`Yii::$app->validator->check($data)`'],
  answer=1,
  why='`DynamicModel` создаёт атрибуты на лету, так что обычные правила работают без отдельного класса. '
      'Удобно для консольных команд и разовых проверок входных данных.',
  ref=('validation', 'Валидация'), node=('rules',)),

# ================================ компоненты, DI, события, поведения, конфиги

q(id='core-di-vs-locator', topic='core',
  text='В чём разница между Service Locator и DI-контейнером?',
  options=['Это два названия одного механизма',
           'Локатор отдаёт готовые компоненты по строковому ID; контейнер создаёт объекты по классу или интерфейсу, разрешая зависимости конструктора',
           'Локатор работает только в консоли',
           'Контейнер хранит объекты, локатор — только конфигурации'],
  answer=1,
  why='`Yii::$app` — локатор: `Yii::$app->db` это «дай компонент с таким ID». Контейнер отвечает на другой '
      'вопрос: «собери мне объект такого типа». Локатор при создании своих компонентов обращается к контейнеру.',
  ref=('di', 'Service Locator и DI-контейнер'), node=('di',)),

q(id='core-setsingleton', topic='core',
  text='Когда стоит брать `setSingleton()` вместо `set()`?',
  options=['Всегда — так быстрее',
           'Когда объект не хранит изменяемого состояния и дорого создаётся: соединение, обёртка над хранилищем',
           'Когда у объекта нет зависимостей',
           'Только для интерфейсов'],
  answer=1,
  why='`set()` собирает новый объект на каждый запрос к контейнеру, `setSingleton()` — один раз за запрос. '
      'Для объекта с изменяемым состоянием общий экземпляр опасен: его поменяют в одном месте, а сломается в другом.',
  ref=('di', 'Service Locator и DI-контейнер'), node=('di',)),

q(id='core-unregistered-iface', topic='core',
  text='Контроллер требует в конструкторе интерфейс, не зарегистрированный в контейнере. Что будет?',
  options=['Подставится `null`',
           'Ошибка создания: контейнер не знает, какой класс взять. Нужно `Yii::$container->set(Iface::class, Impl::class)`',
           'Контейнер найдёт единственную реализацию сам',
           'Интерфейс подставится как строка с именем'],
  answer=1,
  why='Контейнер читает типы параметров конструктора, но интерфейс инстанцировать нельзя, а угадывать '
      'реализацию он не берётся. Связку «интерфейс → класс» объявляют явно, обычно в конфигурации приложения.',
  ref=('di', 'Service Locator и DI-контейнер'), node=('di',)),

q(id='core-behavior-owner', topic='core',
  text='Как поведение узнаёт, к какому объекту оно прикреплено?',
  options=['Через аргумент каждого метода',
           'Через свойство `$this->owner`, которое проставляется при прикреплении',
           'Через `Yii::$app->controller`',
           'Оно этого не знает — работает только со своими данными'],
  answer=1,
  why='`attach($owner)` запоминает владельца и подписывает обработчики из `events()` на его события. '
      'Отсюда поведение и дотягивается до атрибутов модели — например, чтобы проставить дату.',
  ref=('behaviors', 'Поведения'), node=('behaviors',)),

q(id='core-behavior-events', topic='core',
  text='Что возвращает метод `events()` поведения?',
  options=['Список событий, которые поведение само генерирует',
           'Карту «событие владельца → обработчик»: так `TimestampBehavior` цепляется к `beforeInsert` и `beforeUpdate`',
           'Массив имён методов, которые поведение добавляет владельцу',
           'Ничего — метод оставлен для совместимости'],
  answer=1,
  why='Это и есть главный трюк поведений: вместо наследования они подписываются на события чужого объекта. '
      'Отцепили поведение — подписки снялись, владелец об этом даже не знает.',
  ref=('behaviors', 'Поведения'), node=('behaviors',)),

q(id='core-behavior-vs-trait', topic='core',
  text='Чем поведение принципиально отличается от трейта?',
  options=['Ничем, это разные названия одного приёма',
           'Это объект: его настраивают, прикрепляют и отцепляют во время выполнения, и он умеет реагировать на события',
           'Трейт быстрее, потому что не создаёт объект',
           'Поведение работает только с моделями'],
  answer=1,
  why='Трейт вплавляется в класс на этапе компиляции и настроить его нельзя. Поведение — обычный объект '
      'с конфигурацией, поэтому одно и то же поведение с разными настройками вешают на разные модели.',
  ref=('behaviors', 'Поведения'), node=('behaviors',)),

q(id='core-alias-missing', topic='core',
  text='Что вернёт `Yii::getAlias(\'@app/runtime/cache\')`, если такой папки нет?',
  options=['`false`', 'Путь вернётся: метод раскрывает псевдоним, а существование файлов не проверяет',
           '`null`', 'Бросит исключение'],
  answer=1,
  why='Это чистая работа со строками. Исключение будет только на **неизвестном** псевдониме — и то его '
      'можно погасить вторым аргументом `false`.',
  ref=('configurations', 'Конфигурации, псевдонимы, автозагрузка'), node=('app',)),

q(id='core-env-module', topic='core',
  text='Как подключить модуль только в режиме разработки?',
  options=['Задать ему `\'enabled\' => YII_ENV_DEV`',
           'Обернуть подключение в `if (YII_ENV_DEV) { ... }` в конфигурации',
           'Положить модуль в папку `dev/`',
           'Указать `\'on beforeRequest\' => ...`'],
  answer=1,
  why='Конфигурация — обычный PHP-массив, который собирается кодом, поэтому условия в ней законны. '
      'Свойства `enabled` у модулей нет, а шаблоны приложений для этого держат отдельные файлы `web-local.php`.',
  ref=('configurations', 'Конфигурации, псевдонимы, автозагрузка'), node=('app',)),

# ==================================================== кэш вывода и страниц

q(id='state-begincache', topic='state',
  text='Что означает `true`, вернувшееся из `$this->beginCache($id)`?',
  options=['Кэш найден и уже выведен',
           'Кэша нет — содержимое надо сгенерировать, а в конце вызвать `endCache()`',
           'Кэширование отключено в конфигурации',
           'Фрагмент устарел и будет обновлён в фоне'],
  answer=1,
  why='Отсюда и характерная конструкция `if ($this->beginCache($id)) { ... $this->endCache(); }`. '
      'Когда фрагмент нашёлся, метод сам его выводит и возвращает `false` — тело просто пропускается.',
  ref=('caching-output', 'Кэширование фрагментов, страниц и HTTP'), node=('state',)),

q(id='state-renderdynamic', topic='state',
  text='Как показать имя текущего пользователя внутри закэшированного фрагмента?',
  options=['Никак: фрагмент целиком статичен',
           'Через `$this->renderDynamic()` — этот код выполняется при каждом показе, даже из кэша',
           'Добавить имя в `variations`',
           'Уменьшить `duration` до нуля'],
  answer=1,
  why='`variations` создаст отдельный кэш **на каждого пользователя** — для приветствия по имени это '
      'бессмысленная трата памяти. `renderDynamic()` оставляет в кэше дырку, которая заполняется на лету.',
  ref=('caching-output', 'Кэширование фрагментов, страниц и HTTP'), node=('state',)),

q(id='state-etag-304', topic='state',
  text='Что произойдёт, если `ETag` в запросе совпал с тем, что посчитал `HttpCache`?',
  options=['Страница соберётся заново, но не отправится',
           'Ответ 304 Not Modified без тела, действие контроллера не выполняется',
           'Ответ 200 с телом из кэша сервера',
           'Запрос уйдёт в `PageCache`'],
  answer=1,
  why='`HttpCache` — это пре-фильтр: он отвечает 304 ещё до действия. Выигрыш двойной — не тратится и трафик, '
      'и работа сервера. Достаточно одного из `lastModified` или `etagSeed`.',
  ref=('caching-output', 'Кэширование фрагментов, страниц и HTTP'), node=('filters',)),

# ========================================================== вход и права

q(id='sec-identity-iface', topic='sec',
  text='Какие методы требует `IdentityInterface`?',
  options=['`login()`, `logout()`, `getId()`',
           '`findIdentity()`, `findIdentityByAccessToken()`, `getId()`, `getAuthKey()`, `validateAuthKey()`',
           '`authenticate()` и `getRoles()`',
           '`getId()` и `getAuthKey()` — остальное по желанию'],
  answer=1,
  why='Два первых — статические поиски по идентификатору и по токену, `getId()` кладётся в сессию, '
      'а пара `getAuthKey()`/`validateAuthKey()` нужна «запомнить меня»: по ней проверяется кука автологина.',
  ref=('authentication', 'Аутентификация'), node=('user',)),

q(id='sec-remember-me', topic='sec',
  text='Что нужно, чтобы заработало «запомнить меня»?',
  options=['Только галочка в форме входа',
           '`enableAutoLogin = true` у компонента `user`, `duration > 0` в `login()` и рабочие `getAuthKey()`/`validateAuthKey()`',
           'Увеличить `authTimeout`',
           'Хранить сессии в базе'],
  answer=1,
  why='Кука автологина хранит идентификатор и ключ; при следующем визите Yii находит пользователя и '
      'сверяет ключ через `validateAuthKey()`. Если ключ не хранится в базе, проверка не пройдёт — и «запомнить меня» тихо не работает.',
  ref=('authentication', 'Аутентификация'), node=('user',)),

q(id='sec-timeouts', topic='sec',
  text='Чем `authTimeout` отличается от `absoluteAuthTimeout`?',
  options=['Первый для куки, второй для сессии',
           'Первый выбрасывает после N секунд бездействия и продлевается каждым запросом; второй — через N секунд после входа, что бы вы ни делали',
           'Первый в секундах, второй в минутах',
           'Это одно и то же, второе имя устарело'],
  answer=1,
  why='Первый — привычный «тайм-аут простоя» для обычных сайтов. Второй нужен там, где сессия не должна '
      'жить дольше положенного независимо от активности: банк, админка, внутренние системы.',
  ref=('authentication', 'Аутентификация'), node=('user',)),

q(id='sec-rbac-role-perm', topic='sec',
  text='В чём разница между ролью и разрешением в RBAC?',
  options=['Роль проверяют, разрешение назначают',
           'Разрешение — конкретное действие; роль — набор разрешений и других ролей. Проверять лучше разрешения, а назначать роли',
           'Роль хранится в базе, разрешение — в коде',
           'Разрешения нужны только для REST'],
  answer=1,
  why='Проверка `can(\'updatePost\')` переживает смену названий должностей, а `can(\'moderator\')` придётся '
      'переписывать по всему коду, как только появится вторая роль с тем же правом.',
  ref=('authorization', 'Авторизация: ACF и RBAC'), node=('user',)),

q(id='sec-rbac-rule', topic='sec',
  text='Зачем в RBAC нужен класс `Rule`?',
  options=['Чтобы описать иерархию ролей',
           'Для условий, зависящих от параметров: например, `updateOwnPost` проверяет, что автор поста — текущий пользователь',
           'Чтобы задать правила URL для защищённых действий',
           'Чтобы кэшировать иерархию прав'],
  answer=1,
  why='Без правил RBAC отвечает только на вопрос «можно ли вообще», а нужен ответ «можно ли вот это». '
      'Параметры приходят вторым аргументом: `can(\'updatePost\', [\'post\' => $post])`.',
  ref=('authorization', 'Авторизация: ACF и RBAC'), node=('user',)),

# ================================================================ REST и консоль

q(id='api-json-parser', topic='api',
  text='`POST /users` приходит с телом в JSON, но `$model->load(Yii::$app->request->post())` ничего не заполняет. Чего не хватает?',
  options=['Заголовка `Content-Length`',
           'Парсера: `\'parsers\' => [\'application/json\' => \'yii\\web\\JsonParser\']` в компоненте `request`',
           'Правила `safe` у всех атрибутов',
           'Фильтра `ContentNegotiator`'],
  answer=1,
  why='PHP сам разбирает только `application/x-www-form-urlencoded` и `multipart/form-data`. JSON-тело '
      'остаётся сырым потоком, пока его не разберёт парсер — тогда данные появятся в `bodyParams` и `post()`.',
  ref=('rest', 'REST API — быстрый старт'), node=('http',)),

q(id='api-fields-extra', topic='api',
  text='Чем `fields()` отличается от `extraFields()`?',
  options=['`fields()` для чтения, `extraFields()` для записи',
           '`fields()` отдаются всегда (если клиент не сузил список через `?fields=`), `extraFields()` — только по `?expand=`',
           '`extraFields()` доступны только администраторам',
           '`extraFields()` — устаревшее имя `fields()`'],
  answer=1,
  why='Так дорогие связи не грузятся на каждый запрос: список заказов отдаётся плоским, а `?expand=customer` '
      'просит подтянуть клиента. Заодно это единственный способ отдать связь наружу — в `fields()` их не кладут.',
  ref=('rest', 'REST API — быстрый старт'), node=('model',)),

q(id='api-content-negotiation', topic='api',
  text='Как REST-приложение выбирает между JSON и XML?',
  options=['По расширению в адресе',
           'Фильтром `ContentNegotiator` — по заголовку `Accept` или параметру `formatParam`, если формат есть в списке `formats`',
           'По настройке `defaultFormat` — выбора нет',
           'По типу данных, которые вернуло действие'],
  answer=1,
  why='Фильтр стоит первым в `behaviors()` контроллера REST и заодно выбирает язык. Если запрошенного формата '
      'нет в списке `formats`, клиент получит 406 Not Acceptable.',
  ref=('rest-advanced', 'REST API — форматы, аутентификация, версии'), node=('filters',)),

q(id='api-rate-limit', topic='api',
  text='Что нужно, чтобы заработало ограничение частоты запросов?',
  options=['Включить `RateLimiter` в `behaviors()` — больше ничего',
           'Реализовать `RateLimitInterface` (`getRateLimit`, `loadAllowance`, `saveAllowance`) в identity-классе — сам фильтр уже подключён в `yii\\rest\\Controller`',
           'Настроить лимиты в `urlManager`',
           'Поставить расширение yii2-redis'],
  answer=1,
  why='Фильтр знает алгоритм «дырявого ведра», но не знает, где хранить остаток и какие лимиты у этого '
      'пользователя, — это и описывает интерфейс. Не реализован — фильтр просто ничего не делает.',
  ref=('rest-advanced', 'REST API — форматы, аутентификация, версии'), node=('user',)),

# ==================================================== ошибки, инструменты, тесты

q(id='tools-error-action', topic='tools',
  text='Как показывать посетителям свою страницу ошибки вместо стандартной?',
  options=['Переопределить `ErrorHandler::render()`',
           'Указать `errorAction => \'site/error\'` у `errorHandler` и объявить действие через `yii\\web\\ErrorAction`',
           'Положить файл `errors/404.php` в `web/`',
           'Задать `catchAll`'],
  answer=1,
  why='`ErrorAction` кладёт объект исключения в переменную `$exception` представления, а в боевом режиме '
      'сам прячет подробности. `catchAll` — про режим обслуживания, к ошибкам он отношения не имеет.',
  ref=('errors-logging', 'Ошибки и логирование'), node=('app',)),

q(id='tools-log-bootstrap', topic='tools',
  text='Почему компонент `log` добавляют в `bootstrap` приложения?',
  options=['Иначе он не найдёт файл журнала',
           'Компоненты создаются лениво, и без bootstrap сообщения, записанные до первого обращения к `log`, потерялись бы',
           'Чтобы включить `traceLevel`',
           'Это требование PSR-3'],
  answer=1,
  why='Журналирование обязано работать с самой первой строки запроса — в том числе для ошибок на старте. '
      'Поэтому `log` поднимают принудительно, а не ждут первого `Yii::info()`.',
  ref=('errors-logging', 'Ошибки и логирование'), node=('app',)),

q(id='tools-log-filter', topic='tools',
  text='Как отправлять на почту ошибки базы данных, но не 404?',
  options=['Отключить журналирование `yii\\web\\HttpException`',
           'Цель с `levels => [\'error\']` и `categories => [\'yii\\db\\*\']`, а 404 отсечь через `except => [\'yii\\web\\HttpException:404\']`',
           'Фильтровать письма на стороне почтового сервера',
           'Задать `traceLevel => 0`'],
  answer=1,
  why='У каждой цели свои `levels`, `categories` и `except`. Код HTTP приписывается к категории через '
      'двоеточие, поэтому 404 отсекается точечно, не задевая остальные ошибки.',
  ref=('errors-logging', 'Ошибки и логирование')),

q(id='tools-gii-crud', topic='tools',
  text='Что, кроме контроллера, создаёт CRUD-генератор Gii?',
  options=['Только контроллер и миграцию',
           'Поисковую модель (`PostSearch`) и представления `index`, `view`, `create`, `update`, `_form`',
           'Модель, миграцию и тесты',
           'Контроллер и правила URL'],
  answer=1,
  why='Поисковая модель — половина пользы: она даёт `GridView` фильтры и сортировку. Саму AR-модель '
      'генерирует отдельный генератор, из схемы таблицы.',
  ref=('dev-tools', 'Gii, отладчик и другие инструменты')),

q(id='tools-gii-console', topic='tools',
  text='Как сгенерировать модель из консоли?',
  options=['`./yii generate/model post`',
           '`./yii gii/model --tableName=post --modelClass=Post`, если модуль `gii` подключён в консольной конфигурации',
           'Из консоли нельзя — только через браузер',
           '`composer gii model Post`'],
  answer=1,
  why='Gii — обычный модуль, и его генераторы доступны как консольные команды. Это удобно, когда моделей '
      'много: их создание становится строчкой в скрипте, а не десятком кликов.',
  ref=('dev-tools', 'Gii, отладчик и другие инструменты')),

q(id='tools-profile', topic='tools',
  text='Как измерить время выполнения куска кода, чтобы оно попало в панель Profiling отладчика?',
  options=['`microtime()` до и после, а результат — в `Yii::info()`',
           'Обернуть в `Yii::beginProfile(\'метка\')` и `Yii::endProfile(\'метка\')`',
           'Включить `traceLevel`',
           'Вызвать `Yii::$app->log->profile()`'],
  answer=1,
  why='Метки должны совпадать и вкладываться друг в друга — тогда отладчик покажет дерево с длительностями. '
      'Сам фреймворк так же профилирует SQL-запросы и обращения к кэшу.',
  ref=('dev-tools', 'Gii, отладчик и другие инструменты')),

q(id='tools-func-vs-acc', topic='tools',
  text='Чем функциональный тест отличается от приёмочного?',
  options=['Функциональный проверяет одну функцию, приёмочный — весь класс',
           'Функциональный отправляет запрос в приложение напрямую, без браузера и веб-сервера; приёмочный идёт через настоящий браузер и HTTP',
           'Функциональный пишут разработчики, приёмочный — заказчик',
           'Приёмочный работает без базы данных'],
  answer=1,
  why='Отсюда и разница в цене: функциональные быстрые и видят внутренности (`Yii::$app` доступен), '
      'приёмочные медленные, зато проверяют и JavaScript. Ключевые сценарии — приёмочными, остальное — функциональными.',
  ref=('testing', 'Тестирование')),

q(id='tools-fixture-load', topic='tools',
  text='Кто очищает таблицу при работе с `ActiveFixture`?',
  options=['`load()` — он сначала чистит, потом вставляет',
           '`unload()` через `resetTable()`; `load()` только вставляет, а выгрузка вызывается перед загрузкой',
           'Никто — строки накапливаются от теста к тесту',
           'Транзакция, которую открывает модуль Yii2'],
  answer=1,
  why='Со стороны кажется, будто чистит `load()`, но в его коде только вставки. Порядок задаёт `FixtureTrait`: '
      'сначала выгрузка всех фикстур, потом загрузка — поэтому таблица и оказывается пустой.',
  ref=('testing', 'Тестирование')),

q(id='tools-fixture-depends', topic='tools',
  text='Как гарантировать, что связанные фикстуры загрузятся в нужном порядке?',
  options=['Перечислить их в нужном порядке в `_fixtures()`',
           'Указать свойство `depends` — зависимости загружаются раньше',
           'Загружать их вручную в `_before()`',
           'Отключить внешние ключи на время тестов'],
  answer=1,
  why='Порядок в массиве ничего не гарантирует, а внешние ключи не простят вставку заказа раньше клиента. '
      '`depends` описывает зависимость явно, и выгрузка идёт в обратном порядке.',
  ref=('testing', 'Тестирование')),

q(id='tools-debug-n1', topic='tools',
  text='Как быстрее всего найти на странице проблему N+1?',
  options=['Включить `traceLevel` и читать журнал',
           'Открыть панель Database отладчика: десятки одинаковых `SELECT ... WHERE id = ?` — это она и есть',
           'Сравнить время ответа с production',
           'Посмотреть панель Profiling'],
  answer=1,
  why='Панель показывает все запросы страницы, их время и дубликаты. Лечится жадной загрузкой `with()` — '
      'один запрос на связь вместо запроса на каждую строку.',
  ref=('performance', 'Производительность'), node=('ar',)),

q(id='tools-asset-command', topic='tools',
  text='Что делает команда `./yii asset`?',
  options=['Публикует файлы из `sourcePath` в `web/assets`',
           'Объединяет и минифицирует CSS и JS пакетов по конфигурации в единые файлы для production',
           'Чистит папку `web/assets`',
           'Скачивает зависимости фронтенда'],
  answer=1,
  why='Результат — новый файл конфигурации пакетов, который подключают в проде: вместо трёх десятков '
      'запросов за мелкими файлами браузер берёт один. Публикацией занимается `AssetManager` сам, отдельной команды для неё нет.',
  ref=('performance', 'Производительность'), node=('views',)),

# =========================================================== хелперы и памятка

q(id='misc-getvalue', topic='misc',
  text='Как безопасно достать `$user[\'address\'][\'city\']`, если промежуточных ключей может не быть?',
  options=['`$user[\'address\'][\'city\'] ?? $default`',
           '`ArrayHelper::getValue($user, \'address.city\', $default)` — работает и с объектами, и со смесью',
           '`ArrayHelper::index($user, \'address.city\')`',
           '`Json::decode($user)[\'address\'][\'city\']`'],
  answer=1,
  why='Точка задаёт путь любой глубины, а по дороге могут попадаться и массивы, и объекты с геттерами, '
      'и замыкания. Оператор `??` спасает от предупреждения, но пути и объекты не понимает.',
  ref=('helpers', 'Хелперы'), node=('helpers',)),

q(id='misc-map-index', topic='misc',
  text='Чем `ArrayHelper::map()` отличается от `ArrayHelper::index()`?',
  options=['`map()` меняет ключи, `index()` — значения',
           '`map()` строит `[ключ => значение поля]` — для выпадающих списков; `index()` строит `[ключ => весь элемент]`',
           '`map()` работает только с объектами',
           '`index()` сортирует массив'],
  answer=1,
  why='`map($list, \'id\', \'name\')` — это готовый массив для `dropDownList`. `index($list, \'id\')` нужен, когда '
      'элемент понадобится целиком: обращение по ключу вместо перебора.',
  ref=('helpers', 'Хелперы'), node=('helpers',)),

q(id='misc-jsexpression', topic='misc',
  text='Как передать JavaScript-функцию в JSON-настройки виджета?',
  options=['Просто строкой — Yii поймёт',
           'Обернуть в `new JsExpression(\'function () { ... }\')`: `Json::encode()` вставит её без кавычек',
           'Через `Json::htmlEncode()`',
           'Никак: настройки виджета — только данные'],
  answer=1,
  why='`Json::encode()` специально проверяет тип `JsExpression` и выводит содержимое как есть. Обычная строка '
      'стала бы строкой и в JavaScript, а функция — не данные, её нельзя экранировать.',
  ref=('helpers', 'Хелперы'), node=('helpers',)),

q(id='misc-cheatsheet-upload', topic='misc',
  text='Под каким именем сохранять загруженный пользователем файл?',
  options=['Под тем, что прислал клиент, — так удобнее пользователю',
           'Под своим сгенерированным: имя от клиента может содержать путь и опасное расширение',
           'Под именем, пропущенным через `Html::encode()`',
           'Под хешем содержимого — других вариантов нет'],
  answer=1,
  why='Имя из запроса — такие же недоверенные данные, как всё остальное. Настоящее имя, если оно нужно, '
      'хранят отдельным полем в базе и отдают его в заголовке при скачивании.',
  ref=('cheatsheet', 'Памятка на одну страницу'), node=('http',)),

q(id='misc-cheatsheet-goback', topic='misc',
  text='Что делает `$this->goBack()` в контроллере?',
  options=['Отправляет на предыдущую страницу из истории браузера',
           'Ведёт на `returnUrl` пользователя — тот адрес, который запомнил `loginRequired()`, либо на домашнюю страницу',
           'Откатывает транзакцию и возвращает форму',
           'Показывает предыдущее представление без редиректа'],
  answer=1,
  why='Историей браузера сервер не распоряжается. `goBack()` читает `user->returnUrl`, который проставляется '
      'при перехвате гостя; если его нет — сработает `homeUrl`.',
  ref=('cheatsheet', 'Памятка на одну страницу'), node=('user',)),

q(id='misc-cheatsheet-flash', topic='misc',
  text='Какой способ показать сообщение «Сохранено» после редиректа правильный?',
  options=['Передать текст GET-параметром',
           '`Yii::$app->session->setFlash(\'success\', \'Сохранено\')` перед редиректом',
           'Сохранить в `Yii::$app->params`',
           'Отрисовать представление вместо редиректа'],
  answer=1,
  why='Flash живёт ровно один следующий запрос — как раз тот, куда ведёт редирект. Параметр в адресе '
      'останется в истории и в закладках, а `params` не переживает конец запроса.',
  ref=('cheatsheet', 'Памятка на одну страницу'), node=('state',)),

q(id='misc-glossary-dirty', topic='misc',
  text='Что такое «грязные атрибуты» (dirty attributes) в Active Record?',
  options=['Атрибуты, не прошедшие валидацию',
           'Атрибуты, изменённые после загрузки записи, — только они попадают в `UPDATE`',
           'Атрибуты, которых нет в таблице',
           'Атрибуты, помеченные как небезопасные'],
  answer=1,
  why='Модель помнит значения, с которыми её загрузили, и сравнивает их с текущими. Отсюда и поведение '
      '«сохранение без изменений не делает запроса».',
  ref=('glossary', 'Глоссарий'), node=('ar',)),

q(id='misc-glossary-n1', topic='misc',
  text='Что называют проблемой N+1?',
  options=['Один лишний запрос при подсчёте общего числа строк',
           'Один запрос за списком плюс по запросу на связь каждой строки; лечится жадной загрузкой `with()`',
           'Переполнение постраничной навигации на единицу',
           'Лишний JOIN, добавленный `joinWith()`'],
  answer=1,
  why='Сотня заказов на странице — сто запросов за клиентами. `with(\'customer\')` заменяет их одним '
      'запросом `IN (...)`, и время страницы падает на порядок.',
  ref=('glossary', 'Глоссарий'), node=('ar',)),

q(id='misc-glossary-alias', topic='misc',
  text='Что такое псевдоним (alias) в Yii 2?',
  options=['Второе имя класса, объявленное через `class_alias()`',
           'Имя вида `@app` или `@web`, заменяющее путь к папке или адрес; раскрывается через `Yii::getAlias()`',
           'Псевдоним таблицы в SQL-запросе',
           'Короткое имя маршрута'],
  answer=1,
  why='Псевдонимы избавляют от абсолютных путей в конфигурации и работают и для файловой системы (`@app`, '
      '`@runtime`), и для URL (`@web`). Свой добавляют через `Yii::setAlias()`.',
  ref=('glossary', 'Глоссарий'), node=('app',)),

# ==================================================== почта, языки, расширения

q(id='world-file-transport', topic='world',
  text='Как в разработке посмотреть письмо, не отправляя его?',
  options=['Задать `transport => null`',
           '`\'useFileTransport\' => true` — письма лягут в `@runtime/mail` как файлы `.eml`',
           'Подписаться на событие `beforeSend` и прервать отправку',
           'Использовать `sendMultiple()` с пустым списком'],
  answer=1,
  why='Файл `.eml` открывается любым почтовым клиентом, так что видно и вёрстку, и заголовки. Те же письма '
      'показывает панель Mail отладчика.',
  ref=('mailing', 'Отправка почты'), node=('app',)),

q(id='world-mail-views', topic='world',
  text='Где ищутся представления писем при `compose(\'welcome\')`?',
  options=['В `@app/views/mail`',
           'В `viewPath` почтового компонента — по умолчанию `@app/mail`, плюс шаблон `layouts/html`',
           'В папке `views/` рядом с классом письма',
           'В `@runtime/mail`'],
  answer=1,
  why='У писем своя папка и свой шаблон-обёртка, отдельные от представлений сайта: у письма другая вёрстка '
      'и чаще всего две версии — HTML и текст.',
  ref=('mailing', 'Отправка почты')),

q(id='world-sendmultiple', topic='world',
  text='Чем `sendMultiple()` лучше цикла с `send()`?',
  options=['Он отправляет письма параллельно',
           'Одно соединение с SMTP на всю пачку, и метод возвращает число успешно отправленных',
           'Он не требует настройки транспорта',
           'Он складывает адресатов в одно письмо'],
  answer=1,
  why='Рукопожатие с SMTP-сервером дорогое, и на сотне писем разница заметна. Адресаты при этом остаются '
      'у каждого письма свои — это не рассылка в копию.',
  ref=('mailing', 'Отправка почты')),

q(id='world-plurals', topic='world',
  text='Какие формы множественного числа нужны для русского перевода в сообщениях ICU?',
  options=['`one` и `other`',
           '`one`, `few`, `many` (и `other`); `=0` и `=1` — для точных значений',
           '`singular` и `plural`',
           'Формы подбираются автоматически по `language`'],
  answer=1,
  why='В русском три формы: «1 файл», «2 файла», «5 файлов». Английских `one`/`other` не хватает, '
      'а точные ветки `=0` и `=1` полезны для «нет файлов» и «единственный файл».',
  ref=('i18n', 'Интернационализация')),

q(id='world-message-files', topic='world',
  text='Где лежит файл перевода категории `app` на русский при настройках `PhpMessageSource` по умолчанию?',
  options=['`@app/messages/app/ru-RU.php`', '`@app/messages/ru-RU/app.php`',
           '`@app/i18n/ru/app.php`', '`@runtime/messages/ru-RU.php`'],
  answer=1,
  why='Сначала папка языка, внутри — файл категории. Собрать заготовки этих файлов помогает команда '
      '`./yii message` по конфигурации от `./yii message/config`.',
  ref=('i18n', 'Интернационализация')),

q(id='world-ext-type', topic='world',
  text='Зачем расширению тип пакета `yii2-extension` в `composer.json`?',
  options=['Чтобы Composer поставил его в отдельную папку',
           'При установке он попадёт в `vendor/yiisoft/extensions.php`, и Yii сам узнает о его псевдонимах и bootstrap-классе',
           'Чтобы запретить установку без Yii',
           'Это просто метка для сайта пакетов'],
  answer=1,
  why='Файл собирает плагин Composer от Yii. Благодаря ему расширение работает сразу после установки — '
      'не приходится вручную дописывать псевдонимы в конфигурацию приложения.',
  ref=('extensions', 'Расширения и сторонний код')),

q(id='world-ext-bootstrap', topic='world',
  text='Как расширению выполнить код при старте каждого запроса, не требуя правок в конфигурации приложения?',
  options=['Положить код в конструктор главного класса',
           'Через класс с `BootstrapInterface`, указанный в `extra.bootstrap` своего `composer.json`',
           'Подписаться на `beforeRequest` из файла автозагрузки',
           'Никак — пользователь обязан прописать компонент сам'],
  answer=1,
  why='Приложение на старте проходит по `extensions.php` и вызывает `bootstrap($app)` у заявленных классов. '
      'Там расширение регистрирует свои правила URL, псевдонимы и обработчики событий.',
  ref=('extensions', 'Расширения и сторонний код')),

q(id='world-ext-db', topic='world',
  text='Почему в расширении не стоит обращаться к `Yii::$app->db` напрямую?',
  options=['Обращение к компонентам из расширений запрещено',
           'У пользователя данные могут лежать в другом соединении — правильнее объявить настраиваемое свойство `db`',
           '`Yii::$app` недоступен на этапе bootstrap',
           'Это медленнее, чем создать соединение самому'],
  answer=1,
  why='Жёсткая ссылка на компонент с конкретным ID делает расширение негодным для проектов с двумя базами '
      'или с нестандартными именами. Свойство со значением по умолчанию `\'db\'` не мешает никому.',
  ref=('extensions', 'Расширения и сторонний код')),

q(id='world-yii1-app', topic='world',
  text='Чем в Yii 2 заменили `Yii::app()` и `User::model()->findByPk($id)` из Yii 1?',
  options=['`Yii::getApp()` и `User::find($id)`',
           '`Yii::$app` и `User::findOne($id)`',
           '`Yii::instance()` и `User::one($id)`',
           'Ничем — старые вызовы работают и во второй версии'],
  answer=1,
  why='Приложение стало статическим свойством, а у Active Record появились статические `findOne()` и '
      '`findAll()`. Промежуточный объект-«модель» из Yii 1 исчез совсем.',
  ref=('upgrade-from-yii1', 'Переход с Yii 1.1')),

q(id='world-yii1-action', topic='world',
  text='Что изменилось в действиях контроллера при переходе с Yii 1 на Yii 2?',
  options=['Они стали статическими',
           'Они возвращают результат (`return $this->render(...)`) вместо вывода через `echo` — результат становится ответом',
           'Их стало можно объявлять только отдельными классами',
           'Имена методов больше не требуют префикса'],
  answer=1,
  why='Возвращаемое значение проходит через `Response` — поэтому одно и то же действие умеет отдавать и HTML, '
      'и JSON, и редирект. С `echo` этот слой было бы не встроить.',
  ref=('upgrade-from-yii1', 'Переход с Yii 1.1'), node=('controllers',)),

q(id='world-yii1-identity', topic='world',
  text='Что в Yii 2 заменило класс `CUserIdentity` из Yii 1?',
  options=['`yii\\web\\User`',
           '`IdentityInterface`, который реализует сама модель пользователя; проверка пароля переехала в форму входа',
           '`yii\\base\\Security`',
           '`AuthManager`'],
  answer=1,
  why='Отдельного класса-удостоверения больше нет: данные пользователя и так лежат в модели, ей и '
      'реализовывать интерфейс. `yii\\web\\User` — это компонент приложения, он лишь хранит текущую личность.',
  ref=('upgrade-from-yii1', 'Переход с Yii 1.1'), node=('user',)),

q(id='core-event-wildcard', topic='core',
  text='Что делает подписка `$obj->on(\'before*\', $handler)`?',
  options=['Ничего — звёздочка будет воспринята как часть имени события',
           'Подписывает обработчик на все события, имя которых подходит под маску: `beforeSave`, `beforeDelete` и прочие',
           'Подписывает только на событие с именем `before*`',
           'Отписывает все обработчики, начинающиеся на `before`'],
  answer=1,
  why='`Component` хранит маски отдельно и сверяет их через `StringHelper::matchWildcard()`. Обработчики по '
      'маске вызываются перед точными — удобно вешать журналирование сразу на группу событий.',
  ref=('events', 'События'), node=('events',)),

]
